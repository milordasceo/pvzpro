# 📝 Architecture Decision Records (ADR)

> Документация архитектурных решений проекта WB ПВЗ

**Последнее обновление**: 3 ноября 2025

---

## 📋 Содержание

- [О формате ADR](#о-формате-adr)
- [Активные решения](#активные-решения)
- [Шаблон для новых решений](#шаблон-для-новых-решений)
- [История решений](#история-решений)

---

## О формате ADR

Architecture Decision Records (ADR) - это документирование архитектурных решений, которые принимаются в процессе разработки. Каждое решение должно содержать:

1. **Контекст** - какая проблема решается
2. **Решение** - что было выбрано
3. **Обоснование** - почему это решение лучше альтернатив
4. **Последствия** - к чему приведёт это решение
5. **Статус** - принято/отклонено/устарело

---

## Активные решения

### ADR-001: Использование React Native Paper для UI

**Дата**: 20 октября 2025  
**Статус**: ✅ Принято  
**Участники**: Development Team

#### Контекст

Проект требует быстрой разработки с консистентным и профессиональным UI. Необходимо выбрать UI библиотеку, которая:
- Предоставляет готовые компоненты
- Следует современным дизайн-гайдлайнам
- Хорошо интегрируется с React Native
- Имеет активную поддержку и документацию

#### Рассмотренные альтернативы

1. **React Native Paper** (Material Design 3)
   - ✅ Готовые компоненты (40+)
   - ✅ Material Design 3 - современный стандарт
   - ✅ Отличная TypeScript поддержка
   - ✅ Активная разработка
   - ✅ Интеграция с React Navigation

2. **NativeBase**
   - ✅ Универсальный дизайн
   - ❌ Больший размер бандла
   - ❌ Менее активная разработка

3. **React Native Elements**
   - ✅ Легковесный
   - ❌ Устаревший дизайн
   - ❌ Меньше компонентов

4. **Кастомная UI библиотека**
   - ✅ Полный контроль
   - ❌ Высокая стоимость разработки
   - ❌ Необходимость поддержки

#### Решение

Использовать **React Native Paper 5** с Material Design 3.

#### Обоснование

- Material Design 3 - современный и знакомый пользователям дизайн
- Готовые компоненты ускоряют разработку
- Активное комьюнити и регулярные обновления
- Отличная документация на русском языке
- Нативная интеграция с React Navigation 7
- Поддержка кастомных тем
- TypeScript из коробки

#### Последствия

**Положительные**:
- Ускорение разработки UI на 40-50%
- Консистентный дизайн всего приложения
- Меньше кастомного CSS кода
- Готовые анимации и transitions

**Отрицательные**:
- Зависимость от библиотеки (vendor lock-in)
- Необходимость следовать Material Design гайдлайнам
- Размер бандла увеличивается на ~300KB

**Требования**:
- Все UI компоненты должны базироваться на Paper
- Запрещено использовать другие UI библиотеки одновременно
- Кастомизация только через theme API

#### Связанные решения

- [ADR-004: Строгий TypeScript режим](#adr-004-строгий-typescript-режим)

---

### ADR-002: Zustand для State Management

**Дата**: 20 октября 2025  
**Статус**: ✅ Принято  
**Участники**: Development Team

#### Контекст

Приложению необходим state management для:
- Управления состоянием аутентификации
- Хранения данных сотрудников
- Управления уведомлениями
- Глобальных настроек приложения

Требования:
- Простота использования
- Минимальный boilerplate
- TypeScript поддержка
- Малый размер бандла
- Возможность персистентности (AsyncStorage)

#### Рассмотренные альтернативы

1. **Zustand**
   - ✅ Минимальный boilerplate
   - ✅ ~1KB размер
   - ✅ Отличная TypeScript поддержка
   - ✅ Не требует Provider
   - ✅ Простая API

2. **Redux Toolkit**
   - ✅ Проверенное решение
   - ✅ DevTools
   - ❌ Больший boilerplate
   - ❌ ~10KB размер
   - ❌ Требует Provider

3. **MobX**
   - ✅ Реактивность из коробки
   - ❌ Декораторы (stage 2 proposal)
   - ❌ Сложнее для новичков
   - ❌ ~15KB размер

4. **React Context + useReducer**
   - ✅ Нативное решение
   - ❌ Много boilerplate
   - ❌ Performance issues с большими деревьями
   - ❌ Нет persistence из коробы

5. **Recoil**
   - ✅ Atomic state
   - ❌ Ещё в development
   - ❌ Требует RecoilRoot
   - ❌ ~20KB размер

#### Решение

Использовать **Zustand 5** для глобального state management.

#### Обоснование

Zustand предоставляет идеальный баланс между простотой и функциональностью:

```typescript
// Пример store
import { create } from 'zustand';

interface AuthState {
  user: User | null;
  token: string | null;
  login: (credentials: Credentials) => Promise<void>;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  token: null,
  login: async (credentials) => {
    const { user, token } = await api.login(credentials);
    set({ user, token });
  },
  logout: () => set({ user: null, token: null }),
}));
```

Преимущества:
- Минимальный код для создания store
- TypeScript типизация из коробки
- Не нужен Provider - можно использовать напрямую
- Middleware для persistence (persist)
- Middleware для DevTools

#### Последствия

**Положительные**:
- Быстрая разработка stores
- Простота тестирования (stores - обычные объекты)
- Малый размер бандла (+1KB)
- Возможность использовать вне React компонентов

**Отрицательные**:
- Меньше готовых middleware чем у Redux
- Меньше learning resources
- Нет time-travel debugging из коробки (нужен middleware)

**Правила использования**:
1. Один store на доменную область (authStore, employeeStore, etc.)
2. Избегать nested state (держать state flat)
3. Использовать selectors для оптимизации
4. Async операции внутри actions

#### Примеры использования

```typescript
// В компоненте
import { useAuthStore } from '../store/authStore';

const LoginScreen = () => {
  const { login, loading } = useAuthStore();
  
  return (
    <Button onPress={() => login(credentials)}>
      {loading ? 'Loading...' : 'Login'}
    </Button>
  );
};

// С селектором (оптимизация)
const user = useAuthStore((state) => state.user);
```

#### Связанные решения

- [ADR-004: Строгий TypeScript режим](#adr-004-строгий-typescript-режим)

---

### ADR-003: UI-First разработка с Mock данными

**Дата**: 20 октября 2025  
**Статус**: ✅ Принято  
**Участники**: Development Team

#### Контекст

Бэкенд API находится в процессе разработки. Фронтенд-команда не должна быть заблокирована ожиданием готового API. Необходим подход, позволяющий:
- Разрабатывать UI независимо от бэкенда
- Демонстрировать прогресс стейкхолдерам
- Тестировать UX без реальных данных
- Быстро итерировать по дизайну

#### Рассмотренные альтернативы

1. **UI-First с Mock данными**
   - ✅ Независимость от бэкенда
   - ✅ Быстрые итерации
   - ✅ Возможность демонстрации
   - ❌ Необходимость переписывания при интеграции

2. **Backend-First**
   - ✅ Сразу полная интеграция
   - ❌ Блокировка фронтенд-разработки
   - ❌ Невозможность демонстрации UI

3. **API Contract-First (OpenAPI)**
   - ✅ Чёткий контракт
   - ✅ Возможность генерации моков
   - ❌ Требует готового API contract
   - ❌ Больше upfront работы

4. **GraphQL with Apollo Mocks**
   - ✅ Мощные моки
   - ❌ Требует GraphQL на бэкенде
   - ❌ Дополнительная сложность

#### Решение

Использовать **UI-First подход с mock данными** и чёткой изоляцией API слоя.

#### Обоснование

Подход позволяет:
1. Разрабатывать UI параллельно с бэкендом
2. Быстро получать feedback от пользователей
3. Обнаруживать UX проблемы на ранних стадиях
4. Демонстрировать прогресс стейкхолдерам

Архитектура:

```
┌─────────────┐
│  UI Layer   │ ← Screens/Components
└──────┬──────┘
       │
┌──────↓──────┐
│ Hooks Layer │ ← useEmployees(), useAuth()
└──────┬──────┘
       │
┌──────↓──────┐
│ Store Layer │ ← Zustand stores
└──────┬──────┘
       │
┌──────↓──────┐
│   Service   │ ← API calls (с mock режимом)
│   Layer     │
└─────────────┘
```

#### Последствия

**Положительные**:
- Независимость от бэкенда
- Быстрая разработка UI
- Раннее обнаружение UX проблем
- Возможность параллельной работы команд

**Отрицательные**:
- Необходимость создания качественных mock данных
- Риск несоответствия реальному API
- Дополнительная работа при интеграции

**Требования**:
1. Mock данные должны быть реалистичными
2. API сервисы должны поддерживать флаг `useMock`
3. Типы данных должны соответствовать реальному API
4. Документировать различия между mock и real API

#### Примеры реализации

```typescript
// services/api/employeeApi.ts
import { mockEmployees } from '../mocks/employees';

const USE_MOCK = true; // TODO: переключить на false при интеграции

export const employeeApi = {
  async getEmployees() {
    if (USE_MOCK) {
      return Promise.resolve(mockEmployees);
    }
    
    const response = await fetch('/api/employees');
    return response.json();
  },
};
```

#### План миграции на реальный API

1. Разработка UI с mock данными ✅
2. Согласование API contract с бэкендом 🔄
3. Создание типов на основе contract
4. Замена mock на реальные API вызовы
5. Тестирование интеграции
6. Удаление mock кода

#### Связанные решения

- [ADR-002: Zustand для State Management](#adr-002-zustand-для-state-management)

---

### ADR-004: Строгий TypeScript режим

**Дата**: 20 октября 2025  
**Статус**: ✅ Принято  
**Участники**: Development Team

#### Контекст

TypeScript используется для типизации кода. Необходимо определить уровень строгости типизации:
- Базовый режим (minimal type checking)
- Строгий режим (strict mode)
- Постепенная миграция

Цели:
- Минимизировать количество runtime ошибок
- Улучшить developer experience (автокомплит, рефакторинг)
- Сделать код самодокументируемым

#### Рассмотренные альтернативы

1. **Strict Mode с самого начала**
   - ✅ Максимальная безопасность типов
   - ✅ Раннее обнаружение ошибок
   - ❌ Медленнее на старте
   - ❌ Сложнее для новичков

2. **Базовый режим**
   - ✅ Быстрее начать
   - ✅ Проще для новичков
   - ❌ Много потенциальных ошибок
   - ❌ Сложнее мигрировать потом

3. **Постепенная миграция**
   - ✅ Компромисс
   - ❌ Неконсистентный код
   - ❌ Технический долг

#### Решение

Использовать **TypeScript Strict Mode** с самого начала проекта.

#### Обоснование

Strict mode включает:
- `strict: true` - все strict флаги
- `noImplicitAny: true` - запрет на implicit any
- `strictNullChecks: true` - проверка null/undefined
- `strictFunctionTypes: true` - строгая типизация функций
- `strictPropertyInitialization: true` - инициализация свойств классов
- `noImplicitThis: true` - типизация this
- `alwaysStrict: true` - use strict в output

Конфигурация `tsconfig.json`:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

#### Последствия

**Положительные**:
- Раннее обнаружение ошибок (compile-time vs runtime)
- Улучшенный refactoring (IDE уверенно переименовывает)
- Самодокументируемый код (типы как документация)
- Лучший автокомплит в IDE
- Меньше unit тестов для проверки типов

**Отрицательные**:
- Требует больше времени на написание кода
- Необходимость типизировать сторонние библиотеки (@types/*)
- Сложнее для Junior разработчиков
- Иногда нужны type assertions (as)

**Правила**:
1. Запрещён `any` (использовать `unknown` если тип неизвестен)
2. Все функции должны иметь типы параметров и возврата
3. Все API ответы должны быть типизированы
4. Props компонентов должны иметь интерфейсы
5. Используй utility types (Partial, Pick, Omit, etc.)

#### Примеры

```typescript
// ✅ Хорошо
interface User {
  id: string;
  name: string;
  email: string;
}

function getUser(id: string): Promise<User> {
  return api.get<User>(`/users/${id}`);
}

// ❌ Плохо
function getUser(id) {
  return api.get(`/users/${id}`);
}

// ✅ Хорошо: обработка null
const name = user?.name ?? 'Guest';

// ❌ Плохо: без проверки
const name = user.name; // может быть runtime error
```

#### Связанные решения

- [ADR-001: React Native Paper](#adr-001-использование-react-native-paper-для-ui)
- [ADR-002: Zustand](#adr-002-zustand-для-state-management)

---

### ADR-005: Expo вместо Pure React Native

**Дата**: 20 октября 2025  
**Статус**: ✅ Принято  
**Участники**: Development Team

#### Контекст

Необходимо выбрать платформу для разработки мобильного приложения:
- Pure React Native (требует Xcode, Android Studio)
- Expo (managed workflow)
- Expo (bare workflow)

Команда:
- Нет Mac для iOS разработки
- Фокус на быструю разработку
- Необходимость поддержки Android и iOS

#### Рассмотренные альтернативы

1. **Pure React Native**
   - ✅ Полный контроль
   - ✅ Доступ ко всем нативным модулям
   - ❌ Требует Xcode для iOS
   - ❌ Долгая настройка
   - ❌ Сложные обновления

2. **Expo Managed Workflow**
   - ✅ Без нативного кода
   - ✅ Быстрая разработка
   - ✅ OTA updates
   - ❌ Ограниченный набор модулей
   - ❌ Больший размер приложения

3. **Expo Bare Workflow**
   - ✅ Компромисс: Expo + нативный код
   - ✅ Доступ ко всем модулям
   - ❌ Всё ещё требует Xcode/Android Studio

#### Решение

Использовать **Expo SDK 54** (Managed Workflow) с возможностью migration на Bare при необходимости.

#### Обоснование

Expo предоставляет:
- **Быстрый старт**: проект готов за минуты
- **Без Xcode**: можно разрабатывать iOS на Windows
- **OTA Updates**: обновления без App Store review
- **EAS Build**: облачная сборка приложений
- **Expo Go**: тестирование на физических устройствах
- **Большой набор модулей**: camera, location, notifications, etc.
- **Development Client**: расширение возможностей

#### Последствия

**Положительные**:
- Разработка iOS без Mac
- Быстрая разработка и итерации
- Простые обновления зависимостей
- OTA updates для быстрых фиксов
- Облачная сборка (EAS Build)

**Отрицательные**:
- Зависимость от Expo SDK релизов
- Некоторые нативные библиотеки несовместимы
- Больший размер APK/IPA (~30MB overhead)
- Необходимость ждать Expo обновлений для новых RN версий

**План миграции** (если понадобится):
1. Использовать `expo prebuild` для создания android/ios папок
2. Мигрировать на bare workflow
3. Установить нужные нативные модули
4. Продолжить разработку с доступом к нативному коду

#### Используемые Expo модули

- `expo-camera` - работа с камерой (QR коды)
- `expo-location` - геолокация
- `expo-notifications` - push-уведомления
- `expo-image-picker` - выбор изображений
- `expo-document-picker` - выбор документов
- `expo-status-bar` - управление статус-баром
- `@react-native-async-storage/async-storage` - хранилище

#### Связанные решения

- [ADR-001: React Native Paper](#adr-001-использование-react-native-paper-для-ui)

---

## Шаблон для новых решений

Копируйте этот шаблон при добавлении нового ADR:

```markdown
### ADR-XXX: [Название решения]

**Дата**: [ДД месяц ГГГГ]
**Статус**: 🔄 На рассмотрении / ✅ Принято / ❌ Отклонено / ⚠️ Устарело
**Участники**: [Имена участников]

#### Контекст

[Описание проблемы, которую нужно решить]

#### Рассмотренные альтернативы

1. **Вариант 1**
   - ✅ Плюс 1
   - ✅ Плюс 2
   - ❌ Минус 1

2. **Вариант 2**
   - ✅ Плюс 1
   - ❌ Минус 1

#### Решение

[Выбранное решение]

#### Обоснование

[Почему это решение лучше альтернатив]

#### Последствия

**Положительные**:
- [Последствие 1]

**Отрицательные**:
- [Последствие 1]

**Требования**:
- [Требование 1]

#### Связанные решения

- [ADR-XXX: Название](#adr-xxx-название)
```

---

## История решений

| ADR | Название | Дата | Статус |
|-----|----------|------|--------|
| ADR-001 | React Native Paper для UI | 20.10.2025 | ✅ Принято |
| ADR-002 | Zustand для State Management | 20.10.2025 | ✅ Принято |
| ADR-003 | UI-First разработка | 20.10.2025 | ✅ Принято |
| ADR-004 | Строгий TypeScript режим | 20.10.2025 | ✅ Принято |
| ADR-005 | Expo вместо Pure RN | 20.10.2025 | ✅ Принято |

---

## Процесс принятия решений

1. **Выявление проблемы**: команда обнаруживает архитектурный вопрос
2. **Обсуждение**: команда собирается для обсуждения (meeting/Slack)
3. **Исследование**: изучение альтернатив, прототипирование
4. **Создание ADR**: документирование решения по шаблону
5. **Review**: команда review ADR (через PR)
6. **Принятие**: мердж PR = принятие решения
7. **Имплементация**: реализация решения в коде
8. **Ретроспектива**: оценка решения через 1-3 месяца

---

## Дополнительные ресурсы

- [ARCHITECTURE.md](ARCHITECTURE.md) - общая архитектура проекта
- [CONTRIBUTING.md](CONTRIBUTING.md) - процесс разработки
- [ADR GitHub Template](https://github.com/joelparkerhenderson/architecture-decision-record)

---

**Последнее обновление**: 3 ноября 2025  
**Следующий ADR**: ADR-006 (следующее архитектурное решение)
