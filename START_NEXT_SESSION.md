# 🚀 Быстрый старт следующей сессии

**Дата создания:** 20 октября 2025  
**Цель:** Миграция ~509 хардкод цветов на tokens

---

## ⚡ БЫСТРЫЙ СТАРТ (5 минут)

### 1. Прочитай документы (обязательно!)

```bash
# Основной документ - ЧИТАЙ ПЕРВЫМ! ⭐⭐⭐
docs/MIGRATION_PLAN.md

# Карта замены цветов
docs/COLOR_PALETTE.md

# Контекст - почему мы это делаем
docs/COLOR_AUDIT.md
```

### 2. Найди хардкод цвета

```bash
# Запусти скрипт поиска
bash scripts/find-hardcoded-colors.sh

# Должен показать ~509 вхождений
# Топ-10 файлов с хардкодом
# Уникальные hex цвета
```

### 3. Начни миграцию с первого файла

```bash
# Мигрировать ScheduleScreen.tsx (100+ хардкодов)
bash scripts/migrate-colors.sh src/employee/ScheduleScreen.tsx

# Проверить TypeScript
npm run typecheck

# Проверить визуально
npm start
```

---

## 📋 ПЛАН МИГРАЦИИ

### Этап 1: ScheduleScreen.tsx (1-2 часа) 🔴

**Файл:** `src/employee/ScheduleScreen.tsx`  
**Хардкодов:** ~100+

**Шаги:**
1. Запусти: `bash scripts/migrate-colors.sh src/employee/ScheduleScreen.tsx`
2. Добавь import: `import { tokens } from '../ui';` (если нет)
3. Проверь TypeScript: `npm run typecheck`
4. Вручную замени оставшиеся цвета (если есть)
5. Визуально проверь экран
6. Коммит: `refactor(colors): migrate ScheduleScreen to tokens`

### Этап 2: Dashboard экраны (2-3 часа) 🔴

**Файлы:**
- `src/admin/screens/dashboard/StatisticsTab.tsx` (~50)
- `src/admin/screens/dashboard/OnShiftTab.tsx` (~40)
- `src/admin/screens/dashboard/ControlTab.tsx` (~30)

**Для каждого файла:**
1. Мигрируй скриптом
2. Проверь TypeScript
3. Визуально проверь
4. Коммит

### Этап 3: Остальные файлы (1-2 часа) 🟡

**Стратегия:**
1. Найди все файлы: `bash scripts/find-hardcoded-colors.sh`
2. Мигрируй по убыванию количества хардкодов
3. Коммитай группами по функциональности

### Этап 4: Финализация (30 минут) ✅

1. Проверь что не осталось хардкода:
```bash
bash scripts/find-hardcoded-colors.sh
# Должно быть <50 или <10
```

2. Удали backup файлы:
```bash
find src/ -name "*.backup" -delete
```

3. Финальная проверка:
```bash
npm run typecheck
npm start  # визуально проверить
```

4. Создай migration summary

---

## 🗺️ КАРТА ЗАМЕНЫ (частые цвета)

| Хардкод | Замена | Частота |
|---------|--------|---------|
| `#4F46E5` | `tokens.colors.primary.main` | Очень часто |
| `#111827` | `tokens.colors.text.primary` | Очень часто |
| `#6B7280` | `tokens.colors.text.secondary` | Очень часто |
| `#F9FAFB` | `tokens.colors.gray[50]` | Часто |
| `#F3F4F6` | `tokens.colors.gray[100]` | Часто |
| `#E5E7EB` | `tokens.colors.border` | Часто |
| `#FFFFFF` | `tokens.colors.surface` | Часто |
| `#10B981` | `tokens.colors.success.main` | Часто |
| `#F59E0B` | `tokens.colors.warning.main` | Часто |
| `#DC2626` | `tokens.colors.error.main` | Часто |

**Полная карта:** `docs/COLOR_PALETTE.md` (более 50 замен!)

---

## ✅ ЧЕКЛИСТ ДЛЯ КАЖДОГО ФАЙЛА

### Перед миграцией:
- [ ] Создать backup файла (скрипт делает автоматически)
- [ ] Сделать скриншот UI (для сравнения после)
- [ ] Убедиться что нет TypeScript ошибок

### Во время миграции:
- [ ] Запустить скрипт миграции
- [ ] Добавить import tokens (если нужно)
- [ ] Заменить оставшиеся цвета вручную
- [ ] Проверить TypeScript: `npm run typecheck`

### После миграции:
- [ ] Визуально проверить UI (сравнить со скриншотом)
- [ ] Тестировать функциональность
- [ ] Удалить backup если всё ОК
- [ ] Коммит изменений

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Найти все хардкод цвета
bash scripts/find-hardcoded-colors.sh

# Мигрировать файл
bash scripts/migrate-colors.sh <путь к файлу>

# Посчитать хардкоды в конкретном файле
grep -o "#[0-9A-Fa-f]\{6\}" <файл> | wc -l

# Проверка TypeScript
npm run typecheck

# Запуск приложения
npm start
```

---

## 📊 ОТСЛЕЖИВАНИЕ ПРОГРЕССА

### В начале сессии:
```bash
bash scripts/find-hardcoded-colors.sh
# Должно быть ~509
```

### После каждого этапа:
```bash
bash scripts/find-hardcoded-colors.sh
# Смотри как уменьшается число
```

### Целевые метрики:
- **Старт:** 509 хардкодов
- **После Этапа 1:** ~409 (-100)
- **После Этапа 2:** ~289 (-220)
- **Цель:** <50 хардкодов (90% миграция)
- **Идеал:** <10 хардкодов (98% миграция)

---

## ⚠️ ТИПИЧНЫЕ ПРОБЛЕМЫ

### 1. Динамические цвета

```tsx
// ❌ Проблема
const color = isActive ? '#10B981' : '#DC2626';

// ✅ Решение
const color = isActive 
  ? tokens.colors.success.main 
  : tokens.colors.error.main;
```

### 2. TypeScript ошибка: "Cannot find module"

```tsx
// ❌ Если забыл импортировать
backgroundColor: tokens.colors.primary.main,

// ✅ Добавь импорт
import { tokens } from '../ui';
```

### 3. Цвет не найден в tokens

**Сначала проверь:**
- `docs/COLOR_PALETTE.md` - карта замены
- `src/ui/theme/colors.deprecated.ts` - deprecated цвета

**Если не нашёл:**
- Найди ближайший аналог в `tokens.colors.*`
- Запиши в отчёт для обсуждения

---

## 📝 ШАБЛОН КОММИТА

```bash
refactor(colors): migrate <filename> from hardcoded to tokens

- Replace ~XX hardcoded hex colors with tokens.colors.*
- Add tokens import
- Verify visual consistency

Part of color migration initiative (509 → X remaining)
```

**Примеры:**
```bash
git commit -m "refactor(colors): migrate ScheduleScreen to tokens

- Replace ~100 hardcoded hex colors
- Add tokens import from ui
- Verify visual consistency

Part of color migration initiative (509 → 409 remaining)"
```

---

## 🎯 КРИТЕРИИ УСПЕХА

### Минимум (90%):
- ✅ ScheduleScreen.tsx мигрирован
- ✅ Dashboard экраны мигрированы
- ✅ <50 хардкодов осталось
- ✅ 0 визуальных регрессий
- ✅ 0 TypeScript ошибок

### Идеал (98%):
- ✅ Все .tsx файлы мигрированы
- ✅ <10 хардкодов осталось (только обоснованные)
- ✅ Документация обновлена
- ✅ Migration summary создан

---

## 🎉 ПОСЛЕ ЗАВЕРШЕНИЯ

### Создай Migration Summary:

```markdown
# Migration Summary

**Дата:** <дата>
**Мигрировано:** X из 509 (XX%)

## Файлы:
- ScheduleScreen.tsx: 100 → 0
- StatisticsTab.tsx: 50 → 0
- ...

## Осталось:
- X хардкодов в Y файлах

## Проблемы:
- (если были)

## Следующие шаги:
- (если нужны)
```

### Обнови документацию:
- `docs/NEXT_SESSION.md` - отметь миграцию как завершённую
- `docs/MIGRATION_PLAN.md` - добавь результаты

---

## 📚 ДОКУМЕНТЫ ДЛЯ СПРАВКИ

### Обязательные:
1. **`docs/MIGRATION_PLAN.md`** ⭐⭐⭐ - **ЧИТАЙ ПЕРВЫМ!**
2. `docs/COLOR_PALETTE.md` - карта замены цветов
3. `docs/COLOR_AUDIT.md` - почему мы это делаем

### Справочные:
4. `docs/UI_TOKENS.md` - все design tokens
5. `docs/UI_GUIDELINES.md` - правила использования
6. `src/ui/theme/colors.ts` - финальная палитра (20 цветов)
7. `src/ui/theme/colors.deprecated.ts` - deprecated цвета

---

## 💡 СОВЕТЫ

### ⚡ Ускорение миграции:
1. Используй скрипты (автоматизация 80% работы)
2. Мигрируй по одному файлу за раз
3. Коммить после каждого файла
4. Не пропускай проверку TypeScript

### 🎯 Фокус:
1. Начни с ScheduleScreen (самый приоритетный)
2. Потом Dashboard (админка важна)
3. Остальное по убыванию

### 🛡️ Безопасность:
1. Скрипт создаёт backup автоматически
2. Визуально проверяй каждый экран
3. Тестируй функциональность
4. TypeScript отловит ошибки типов

---

## ⏱️ ТАЙМИНГ

| Этап | Время | Файлов | Хардкодов |
|------|-------|---------|-----------|
| Подготовка | 15 мин | - | - |
| Этап 1 | 1-2 часа | 1 | ~100 |
| Этап 2 | 2-3 часа | 3 | ~120 |
| Этап 3 | 1-2 часа | ~10 | ~290 |
| Этап 4 | 30 мин | - | - |
| **ИТОГО** | **5-7 часов** | **~14** | **~509** |

---

## 🚀 СТАРТ!

**Твой первый шаг:**

```bash
# 1. Открой основной план
code docs/MIGRATION_PLAN.md

# 2. Найди хардкод
bash scripts/find-hardcoded-colors.sh

# 3. Начни миграцию
bash scripts/migrate-colors.sh src/employee/ScheduleScreen.tsx
```

---

**Удачи! 🎉**

**Помни:** Скрипты делают 80% работы, ты проверяешь и доводишь до совершенства!

---

**P.S.** Если возникнут вопросы - смотри `docs/MIGRATION_PLAN.md` (там ВСЁ!)

