# 🎨 Dev UI Catalog FAB

**Плавающая кнопка для быстрого доступа к UI Catalog из любого места приложения**

---

## 🎯 Назначение

Позволяет разработчикам быстро открыть UI Catalog Screen без необходимости переключения между табами.

---

## ✨ Особенности

- ✅ **Только в DEV режиме** - автоматически скрывается в production
- ✅ **Тройной тап** для открытия - не мешает работе с приложением
- ✅ **Перетаскивание** - можно переместить в удобное место
- ✅ **Полупрозрачная** - не закрывает контент
- ✅ **Глобальная** - отображается на всех экранах
- ✅ **Portal** - всегда поверх контента

---

## 🚀 Использование

### Автоматическое подключение

FAB уже подключена в `AppProvider.tsx` и автоматически отображается во всём приложении:

```tsx
<NavigationContainer>
  {children}
  {/* FAB для быстрого доступа к UI Catalog (только в DEV) */}
  <DevUICatalogFAB />
</NavigationContainer>
```

### Как открыть UI Catalog

**Способ 1: Тройной тап на FAB** (рекомендуется)
1. Найдите плавающую кнопку с иконкой палитры 🎨
2. Быстро нажмите 3 раза подряд
3. Откроется UI Catalog

**Способ 2: Перейти вручную**
1. Админ панель → Таб "UI"

---

## 🎨 Внешний вид

- **Позиция:** Правый нижний угол
- **Цвет:** Primary (фиолетовый)
- **Размер:** 56x56 px
- **Иконка:** Палитра (`palette`)
- **Индикатор тапов:** Появляется при нажатии (показывает прогресс до 3 тапов)

---

## 🔧 Настройка

### Скрыть FAB вручную

```tsx
<DevUICatalogFAB visible={false} />
```

### Изменить условие отображения

```tsx
// В AppProvider.tsx
<DevUICatalogFAB visible={__DEV__ && someCondition} />
```

---

## 📱 Взаимодействие

### Перетаскивание
- Зажмите и удерживайте FAB
- Перетащите в удобное место
- Позиция сохраняется до перезагрузки

### Тройной тап
1. **Первый тап** - появляется индикатор
2. **Второй тап** - индикатор остаётся
3. **Третий тап** - открывается UI Catalog
4. Если пауза > 800мс - счётчик сбрасывается

---

## 🎯 Навигация

### Успешная навигация
Если текущая навигация поддерживает переход на экран "ПВЗ" (который временно заменён на UICatalogScreen):
```tsx
navigation.navigate('ПВЗ');
```

### Fallback
Если навигация недоступна (например, другой стек навигации), показывается Alert с подсказкой:
```
🎨 UI Catalog
Откройте Админ панель → Таб "UI" для просмотра каталога компонентов
```

---

## 💡 Примеры использования

### Во время разработки

**Сценарий 1: Проверить цвет**
1. Тройной тап на FAB
2. Вкладка "Цвета"
3. Найти нужный цвет
4. Скопировать hex код

**Сценарий 2: Посмотреть компонент**
1. Тройной тап на FAB
2. Вкладка "Компоненты"
3. Найти нужный компонент
4. Посмотреть примеры использования

**Сценарий 3: Проверить размеры**
1. Тройной тап на FAB
2. Вкладка "Отступы"
3. Найти нужный spacing/radius
4. Использовать в коде

---

## 🔍 Технические детали

### Зависимости
- `react-native-paper` (Portal)
- `@react-navigation/native` (NavigationProp)
- `@expo/vector-icons` (MaterialCommunityIcons)

### Анимация
- Pan gesture для перетаскивания
- Animated.ValueXY для позиции
- PanResponder для обработки жестов

### Состояние
- `tapCount` - счётчик тапов (0-3)
- `pan` - позиция FAB (x, y)

---

## ⚠️ Важно

### Production режим
FAB **автоматически скрывается** в production сборке благодаря проверке `__DEV__`:

```tsx
visible = __DEV__  // false в production
```

### Производительность
- Мемоизирован через `React.memo`
- Portal рендерится только один раз
- Анимации используют `useNativeDriver: false` (из-за layout анимаций)

---

## 🎨 Кастомизация стилей

Все стили находятся в `StyleSheet.create()`:

```tsx
fab: {
  width: 56,
  height: 56,
  borderRadius: 28,
  backgroundColor: tokens.colors.primary.main,
  opacity: 0.8,  // Полупрозрачность
}
```

Можно изменить:
- `backgroundColor` - цвет кнопки
- `opacity` - прозрачность
- `bottom` - позицию по вертикали
- `right` - позицию по горизонтали

---

## 🐛 Troubleshooting

### FAB не отображается
- Проверьте, что вы в DEV режиме
- Проверьте, что `visible={true}` (по умолчанию)
- Проверьте консоль на ошибки

### Не открывается UI Catalog
- Убедитесь, что вы в Admin навигации
- Попробуйте перейти вручную: Админ панель → "UI"
- Проверьте настройки навигации

### FAB перекрывает контент
- Перетащите её в другое место
- Уменьшите `opacity` в стилях
- Временно скройте: `<DevUICatalogFAB visible={false} />`

---

## 📚 См. также

- [`UICatalogScreen.tsx`](../admin/screens/UICatalogScreen.tsx) - сам каталог
- [`UI_COMPONENTS.md`](../../docs/UI_COMPONENTS.md) - документация компонентов
- [`UI_TOKENS.md`](../../docs/UI_TOKENS.md) - документация токенов

---

**Быстрый доступ к UI системе в любое время!** 🚀

