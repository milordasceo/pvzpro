# 🏗️ Архитектура модуля "Сотрудники"

**Дата:** 2 ноября 2025  
**Версия:** 1.0  
**Последнее обновление:** 2025-11-04 (структура документации)

---

## 📐 Общая структура

```
┌─────────────────────────────────────────────────────────────┐
│                     МОДУЛЬ "СОТРУДНИКИ"                      │
│                   (Admin Dashboard)                          │
└─────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            │                 │                 │
       ┌────▼────┐      ┌────▼────┐      ┌────▼────┐
       │ Список  │      │ Детали  │      │  Форма  │
       │Employees│      │Employee │      │Employee │
       │ Screen  │      │ Details │      │  Form   │
       └────┬────┘      └────┬────┘      └────┬────┘
            │                │                 │
            └────────┬───────┴─────────────────┘
                     │
                ┌────▼────┐
                │UI Layer │
                │Components│
                └────┬────┘
                     │
                ┌────▼────┐
                │Business │
                │  Logic  │
                │ (Hooks) │
                └────┬────┘
                     │
                ┌────▼────┐
                │   Data  │
                │  Layer  │
                │(Services│
                │   API)  │
                └─────────┘
```

---

## 🎯 Компоненты и их взаимодействие

### 1. Экран списка (EmployeesScreen)

```
┌─────────────────────────────────────────────────┐
│           EmployeesScreen (Список)               │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  SearchInput + IconButton (Фильтр)        │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeFilters (выпадающая панель)      │  │
│  │  - Статус                                  │  │
│  │  - ПВЗ                                     │  │
│  │  - На смене / Не на смене                 │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  FlatList                                  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │  EmployeeCard                       │  │  │
│  │  │  - Аватар + Имя                      │  │  │
│  │  │  - Должность + ПВЗ                   │  │  │
│  │  │  - Статус                            │  │  │
│  │  │  - Кнопки действий (Чат, Задача)    │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │  EmployeeCard                       │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  └─────────────...─────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
└─────────────────────────────────────────────────┘
         │
         │ onClick(employee)
         ▼
   [Navigate to Details]
```

**Используемые хуки:**

- `useEmployees(filters)` - получение списка с фильтрами
- `useState` - управление состоянием поиска и фильтров

**Данные:**

```typescript
{
  employees: AdminEmployee[],    // Список сотрудников
  loading: boolean,               // Идёт загрузка?
  error: string | null,           // Ошибка загрузки
  refresh: () => void,            // Обновить список
}
```

---

### 2. Экран деталей (EmployeeDetailsScreen)

```
┌─────────────────────────────────────────────────┐
│        EmployeeDetailsScreen (Детали)            │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeHeader                           │  │
│  │  - Большой аватар (80x80)                 │  │
│  │  - Имя, должность                         │  │
│  │  - Статус (бейдж)                          │  │
│  │  - Кнопки: [Чат] [Звонок]                │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeStats (4 карточки в сетке)       │  │
│  │  ┌──────────┐  ┌──────────┐               │  │
│  │  │ Смен: 48 │  │ Часов:384│               │  │
│  │  └──────────┘  └──────────┘               │  │
│  │  ┌──────────┐  ┌──────────┐               │  │
│  │  │Рейтинг:4.8│ │ Задач:123│               │  │
│  │  └──────────┘  └──────────┘               │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeInfoSection "Контакты"           │  │
│  │  📞 Телефон: +7 999 123 45 67            │  │
│  │  ✉️  Email: ivan@example.com             │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeInfoSection "Работа"             │  │
│  │  👤 Должность: Сотрудник ПВЗ             │  │
│  │  📍 ПВЗ: ПВЗ №1 (ТЦ Мега)                │  │
│  │  📅 Дата найма: 15 января 2024           │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeInfoSection "Финансы"            │  │
│  │  💰 Заработано: 45 000 ₽                 │  │
│  │  🎁 Премии: +5 000 ₽                     │  │
│  │  ⚠️  Штрафы: -1 000 ₽                    │  │
│  │  💵 Итого: 49 000 ₽                      │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  EmployeeShiftHistory                     │  │
│  │  ✅ 01.11.2025 - 8 ч 30 мин              │  │
│  │  ✅ 30.10.2025 - 9 ч 15 мин              │  │
│  │  ✅ 28.10.2025 - 8 ч 00 мин              │  │
│  │  [Показать всё] ────────────────────      │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
├─────────────────────────────────────────────────┤
│  [Редактировать]  [Удалить]                     │
└─────────────────────────────────────────────────┘
         │
         │ onClick(Редактировать)
         ▼
   [Navigate to Form]
```

**Используемые хуки:**

- `useEmployeeDetails(employeeId)` - получение деталей
- `useEmployeeActions()` - действия (удалить, изменить статус)

**Данные:**

```typescript
{
  employee: AdminEmployee | null,   // Данные сотрудника
  loading: boolean,                  // Идёт загрузка?
  error: string | null,              // Ошибка загрузки
  refresh: () => void,               // Обновить данные
}
```

---

### 3. Форма добавления/редактирования (EmployeeFormScreen)

```
┌─────────────────────────────────────────────────┐
│      EmployeeFormScreen (Форма)                  │
│      [Новый сотрудник] или [Редактирование]      │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  Фото профиля (кликабельно)               │  │
│  │       ┌─────────┐                          │  │
│  │       │  [📷]   │ ← Загрузить/изменить    │  │
│  │       └─────────┘                          │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  Основная информация                       │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Имя и фамилия *                     │  │  │
│  │  │ [___________________________]        │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Телефон *                           │  │  │
│  │  │ [+7 999 123 45 67]                  │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Email (опционально)                 │  │  │
│  │  │ [example@email.com]                 │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  Должность и статус                        │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Должность *                         │  │  │
│  │  │ [Сотрудник ▼]                       │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Статус занятости                    │  │  │
│  │  │ [Выходной ▼]                        │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ ☑️ Активен (в штате)                │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  Привязка к ПВЗ                            │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Выберите ПВЗ *                      │  │  │
│  │  │ [ПВЗ №1 (ТЦ Мега) ▼]                │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │  Дополнительно                             │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Дата найма                          │  │  │
│  │  │ [15.01.2024 📅]                     │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ Базовая зарплата (₽/мес)            │  │  │
│  │  │ [45 000]                            │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
├─────────────────────────────────────────────────┤
│  [Отмена]                      [Сохранить]      │
└─────────────────────────────────────────────────┘
         │
         │ onClick(Сохранить)
         ▼
   [Validate & Submit]
         │
         ▼
   [Navigate back]
```

**Используемые хуки:**

- `useEmployeeForm(employeeId?)` - управление формой
  - `formData` - данные формы
  - `errors` - ошибки валидации
  - `validate()` - проверить форму
  - `submit()` - отправить данные

**Валидация:**

```typescript
// Обязательные поля:
- name: не пустое, минимум 2 символа
- phone: корректный формат телефона
- pvzId: выбран ПВЗ из списка
- position: выбрана должность

// Опциональные поля:
- email: если заполнен, проверить формат
- baseSalary: если заполнен, положительное число
```

---

## 🔄 Потоки данных

### Поток 1: Загрузка списка сотрудников

```
[EmployeesScreen]
       │
       │ useEmployees(filters)
       ▼
  [useEmployees hook]
       │
       │ fetchEmployees()
       ▼
 [employeeService.getAll(filters)]
       │
       │ API: GET /employees?status=active&pvzId=123
       ▼
  [Backend API]
       │
       │ Response: { data: AdminEmployee[] }
       ▼
  [useEmployees hook]
       │
       │ setEmployees(data)
       ▼
 [EmployeesScreen]
       │
       │ Render FlatList
       ▼
  [EmployeeCard ×N]
```

### Поток 2: Просмотр деталей сотрудника

```
[EmployeesScreen]
       │
       │ onPress(employee)
       ▼
navigation.navigate('EmployeeDetails', { employeeId })
       │
       ▼
[EmployeeDetailsScreen]
       │
       │ useEmployeeDetails(employeeId)
       ▼
  [useEmployeeDetails hook]
       │
       │ fetchEmployee()
       ▼
 [employeeService.getById(employeeId)]
       │
       │ API: GET /employees/:id
       ▼
  [Backend API]
       │
       │ Response: { data: AdminEmployee }
       ▼
  [useEmployeeDetails hook]
       │
       │ setEmployee(data)
       ▼
 [EmployeeDetailsScreen]
       │
       │ Render детали
       ▼
  [EmployeeHeader, EmployeeStats, etc.]
```

### Поток 3: Создание/редактирование сотрудника

```
[EmployeeDetailsScreen] или [EmployeesScreen]
       │
       │ onPress(Редактировать/Добавить)
       ▼
navigation.navigate('EmployeeForm', { employeeId? })
       │
       ▼
[EmployeeFormScreen]
       │
       │ useEmployeeForm(employeeId?)
       ▼
  [useEmployeeForm hook]
       │
       │ (если employeeId) loadEmployee()
       │ (иначе) initEmptyForm()
       ▼
  [EmployeeFormScreen]
       │
       │ Пользователь заполняет форму
       │ onChange(field, value)
       ▼
  [useEmployeeForm hook]
       │
       │ setFormData({ ...formData, [field]: value })
       ▼
  [EmployeeFormScreen]
       │
       │ onPress(Сохранить)
       ▼
  [useEmployeeForm hook]
       │
       │ validate() → errors?
       │ (если есть ошибки) ✗ return
       │ (если нет) ✓ continue
       ▼
  [useEmployeeForm hook]
       │
       │ submit()
       ▼
 [employeeService.create() или .update()]
       │
       │ API: POST /employees или PUT /employees/:id
       ▼
  [Backend API]
       │
       │ Response: { data: AdminEmployee }
       ▼
  [useEmployeeForm hook]
       │
       │ return success
       ▼
[EmployeeFormScreen]
       │
       │ navigation.goBack()
       ▼
[EmployeeDetailsScreen] или [EmployeesScreen]
       │
       │ refresh()
       ▼
  (список обновлён)
```

### Поток 4: Удаление сотрудника

```
[EmployeeDetailsScreen]
       │
       │ onPress(Удалить)
       ▼
  Alert.alert('Подтверждение')
       │
       │ onConfirm
       ▼
  [useEmployeeActions hook]
       │
       │ deleteEmployee(employeeId)
       ▼
 [employeeService.delete(employeeId)]
       │
       │ API: DELETE /employees/:id
       ▼
  [Backend API]
       │
       │ Response: 204 No Content
       ▼
  [useEmployeeActions hook]
       │
       │ return success
       ▼
[EmployeeDetailsScreen]
       │
       │ navigation.goBack()
       ▼
[EmployeesScreen]
       │
       │ refresh()
       ▼
  (сотрудник удалён из списка)
```

---

## 🗂️ Структура файлов с зависимостями

```
src/admin/screens/employees/
│
├── index.ts                                    # Публичный API модуля
│   └── export { EmployeesScreen, EmployeeDetailsScreen, EmployeeFormScreen }
│
├── EmployeesScreen.tsx                         # Экран списка
│   ├── import: EmployeeCard
│   ├── import: EmployeeFilters
│   ├── import: SearchInput, EmptyState, ErrorState (from ui)
│   └── hook: useEmployees
│
├── EmployeeDetailsScreen.tsx                   # Экран деталей
│   ├── import: EmployeeHeader
│   ├── import: EmployeeStats
│   ├── import: EmployeeInfoSection
│   ├── import: EmployeeShiftHistory
│   ├── import: LoadingState, ErrorState, Button (from ui)
│   ├── hook: useEmployeeDetails
│   └── hook: useEmployeeActions
│
├── EmployeeFormScreen.tsx                      # Форма
│   ├── import: TextInput, SelectModal, Button (from ui)
│   └── hook: useEmployeeForm
│
├── components/
│   ├── EmployeeCard.tsx                       # Карточка в списке
│   │   ├── import: Avatar, Text (from react-native-paper)
│   │   └── import: tokens (from ui)
│   │
│   ├── EmployeeFilters.tsx                    # Фильтры
│   │   ├── import: SelectModal (from ui)
│   │   └── import: tokens (from ui)
│   │
│   ├── EmployeeHeader.tsx                     # Шапка профиля
│   │   ├── import: Avatar, Text (from react-native-paper)
│   │   ├── import: IconButton (from ui)
│   │   └── import: tokens (from ui)
│   │
│   ├── EmployeeStats.tsx                      # Статистика
│   │   ├── import: Text (from react-native-paper)
│   │   └── import: tokens (from ui)
│   │
│   ├── EmployeeInfoSection.tsx                # Блок информации
│   │   ├── import: Text (from react-native-paper)
│   │   └── import: tokens (from ui)
│   │
│   └── EmployeeShiftHistory.tsx               # История смен
│       ├── import: Text (from react-native-paper)
│       ├── import: Button (from ui)
│       └── import: tokens (from ui)
│
├── hooks/
│   ├── useEmployees.ts                        # Получение списка
│   │   ├── import: AdminEmployee (from types)
│   │   └── import: employeeService
│   │
│   ├── useEmployeeDetails.ts                  # Получение деталей
│   │   ├── import: AdminEmployee (from types)
│   │   └── import: employeeService
│   │
│   ├── useEmployeeForm.ts                     # Управление формой
│   │   ├── import: EmployeeFormData, EmployeeFormErrors (from types)
│   │   └── import: employeeService
│   │
│   └── useEmployeeActions.ts                  # Действия
│       └── import: employeeService
│
└── types/
    └── employee.types.ts                      # Типы модуля
        ├── export: EmployeeFormData
        ├── export: EmployeeFormErrors
        ├── export: EmployeeAction
        └── export: EmployeeActionType
```

---

## 🔌 API Endpoints

### Получение списка сотрудников

```
GET /api/employees

Query params:
  - pvzId: string (опционально)
  - status: 'all' | 'active' | 'inactive' (опционально)
  - onShift: boolean (опционально)
  - search: string (опционально)

Response:
{
  data: AdminEmployee[],
  total: number,
  page: number,
  perPage: number
}
```

### Получение сотрудника по ID

```
GET /api/employees/:id

Response:
{
  data: AdminEmployee
}
```

### Создание сотрудника

```
POST /api/employees

Body:
{
  name: string,
  phone: string,
  email?: string,
  position: string,
  pvzId: string,
  hiredAt: Date,
  ...
}

Response:
{
  data: AdminEmployee
}
```

### Обновление сотрудника

```
PUT /api/employees/:id

Body:
{
  name?: string,
  phone?: string,
  email?: string,
  ...
}

Response:
{
  data: AdminEmployee
}
```

### Удаление сотрудника

```
DELETE /api/employees/:id

Response:
204 No Content
```

### Изменение статуса

```
PATCH /api/employees/:id/status

Body:
{
  employmentStatus: 'working' | 'day_off' | 'sick_leave' | 'vacation' | 'fired',
  isActive: boolean
}

Response:
{
  data: AdminEmployee
}
```

---

## 🎨 UI/UX Паттерны

### Цветовая схема

```typescript
// Статусы сотрудников
const statusColors = {
  working: '#10B981', // Зелёный - на смене
  day_off: '#9CA3AF', // Серый - выходной
  sick_leave: '#F59E0B', // Оранжевый - больничный
  vacation: '#3B82F6', // Синий - отпуск
  fired: '#EF4444', // Красный - уволен
};

// Должности
const positionColors = {
  trainee: '#3B82F6', // Синий
  employee: '#8B5CF6', // Фиолетовый
  senior: '#F59E0B', // Оранжевый
  manager: '#10B981', // Зелёный
};
```

### Размеры и отступы

```typescript
const sizes = {
  // Аватары
  avatarList: 60,
  avatarDetails: 80,
  avatarForm: 100,

  // Отступы
  screenPadding: 16,
  cardPadding: 16,
  cardGap: 12,
  sectionGap: 24,

  // Радиусы
  cardRadius: 12,
  buttonRadius: 8,
  badgeRadius: 4,
};
```

### Типографика

```typescript
// Заголовки
headlineSmall: { fontSize: 24, fontWeight: '600' }
titleMedium: { fontSize: 16, fontWeight: '600' }
titleLarge: { fontSize: 22, fontWeight: '700' }

// Основной текст
bodyMedium: { fontSize: 14 }
bodySmall: { fontSize: 12 }

// Вторичный текст
caption: { fontSize: 11, color: secondary }
```

---

## ✅ Чеклист архитектуры

- [x] ✅ Определена структура папок и файлов
- [x] ✅ Определены все компоненты и их взаимодействие
- [x] ✅ Определены хуки и их зависимости
- [x] ✅ Определены потоки данных
- [x] ✅ Определены API endpoints
- [x] ✅ Определены типы данных
- [x] ✅ Определены UI/UX паттерны
- [ ] ⏳ Реализованы все компоненты
- [ ] ⏳ Реализованы все хуки
- [ ] ⏳ Интегрированы с API
- [ ] ⏳ Добавлены тесты

---

## 🚀 Готово к реализации!

Эта архитектура обеспечивает:

- ✅ **Модульность** - легко добавлять новые функции
- ✅ **Переиспользуемость** - компоненты можно использовать повторно
- ✅ **Масштабируемость** - легко расширять функциональность
- ✅ **Тестируемость** - каждый компонент можно тестировать отдельно
- ✅ **Понятность** - чёткое разделение ответственности

**Можно начинать разработку по этому плану!** 🎯
