# 🔄 План миграции хардкод цветов на tokens

**Дата создания:** 20 октября 2025  
**Статус:** Готов к выполнению

---

## 🎯 Цель

Заменить все **~509 хардкод цветов** в проекте на `tokens.colors.*`

---

## 📊 Текущая ситуация

### Найдено хардкод цветов:

| Файл | Вхождений | Приоритет |
|------|-----------|-----------|
| `src/employee/ScheduleScreen.tsx` | ~100+ | 🔴 Высокий |
| `src/admin/screens/dashboard/StatisticsTab.tsx` | ~50+ | 🔴 Высокий |
| `src/admin/screens/dashboard/OnShiftTab.tsx` | ~40+ | 🔴 Высокий |
| `src/admin/screens/dashboard/ControlTab.tsx` | ~30+ | 🟡 Средний |
| Остальные файлы | ~300+ | 🟢 Низкий |

**Всего:** ~509 вхождений

---

## 🗺️ Карта замены цветов

### Частые замены (автоматизируемые):

| Хардкод | Замена | Частота |
|---------|--------|---------|
| `#4F46E5` | `tokens.colors.primary.main` | Очень часто |
| `#111827` | `tokens.colors.text.primary` | Очень часто |
| `#6B7280` | `tokens.colors.text.secondary` | Очень часто |
| `#F9FAFB` | `tokens.colors.gray[50]` | Часто |
| `#F3F4F6` | `tokens.colors.gray[100]` | Часто |
| `#E5E7EB` | `tokens.colors.border` | Часто |
| `#FFFFFF` | `tokens.colors.surface` | Часто |
| `#10B981` | `tokens.colors.success.main` | Часто |
| `#F59E0B` | `tokens.colors.warning.main` | Часто |
| `#DC2626` | `tokens.colors.error.main` | Часто |
| `#9CA3AF` | `tokens.colors.text.muted` | Средне |
| `#D1D5DB` | `tokens.colors.gray[300]` | Средне |

### Специальные замены (требуют проверки):

| Хардкод | Возможная замена | Примечание |
|---------|------------------|------------|
| `#EF4444` | `tokens.colors.error.main` | red-500 → error.main |
| `#16A34A` | `tokens.colors.success.main` | green-600 → success.main |
| `#D1FAE5` | `tokens.colors.success.light` | green-100 |
| `#FEE2E2` | `tokens.colors.error.light` | red-100 |
| `#FEF3C7` | `tokens.colors.warning.light` | amber-100 |
| `#ECFDF5` | `tokens.colors.success.light` | green-50 → light |
| `#FEF2F2` | `tokens.colors.error.light` | red-50 → light |
| `#EFF6FF` | `tokens.colors.primary.light` | blue-50 → primary |
| `#BFDBFE` | `tokens.colors.primary.light` | blue-200 → primary |
| `#1E40AF` | `tokens.colors.primary.main` | blue-800 → primary |
| `#92400E` | `tokens.colors.warning.main` | amber-900 → main |
| `#991B1B` | `tokens.colors.error.main` | red-800 → main |

### Удалённые цвета (заменить на аналоги):

| Хардкод | Была категория | Новая замена |
|---------|----------------|--------------|
| `#0891B2` | info.main | `tokens.colors.primary.main` |
| `#CFFAFE` | info.light | `tokens.colors.primary.light` |
| `#0E7490` | info.dark | `tokens.colors.primary.main` |
| Любой info | - | Используй primary |

---

## 📋 План миграции по этапам

### Этап 1: ScheduleScreen.tsx (1-2 часа)

**Файл:** `src/employee/ScheduleScreen.tsx`  
**Вхождений:** ~100+  
**Сложность:** Высокая

**Подход:**
1. Создать backup файла
2. Импортировать tokens
3. Заменить частые цвета (автоматически)
4. Проверить визуально
5. Заменить специальные цвета (вручную)
6. Тестировать функциональность

**Автоматизация:**
```bash
# Backup
cp src/employee/ScheduleScreen.tsx src/employee/ScheduleScreen.tsx.backup

# Замены (основные)
sed -i "s/#4F46E5/tokens.colors.primary.main/g" src/employee/ScheduleScreen.tsx
sed -i "s/#111827/tokens.colors.text.primary/g" src/employee/ScheduleScreen.tsx
sed -i "s/#6B7280/tokens.colors.text.secondary/g" src/employee/ScheduleScreen.tsx
# ... и т.д.
```

---

### Этап 2: Dashboard файлы (2-3 часа)

**Файлы:**
- `src/admin/screens/dashboard/StatisticsTab.tsx` (~50)
- `src/admin/screens/dashboard/OnShiftTab.tsx` (~40)
- `src/admin/screens/dashboard/ControlTab.tsx` (~30)

**Подход:** Аналогичный этапу 1

---

### Этап 3: Остальные файлы (1-2 часа)

**Стратегия:**
1. Найти все файлы с хардкодом
2. Сгруппировать по количеству вхождений
3. Мигрировать по очереди
4. Проверять TypeScript после каждого

---

### Этап 4: Проверка и чистка (30 минут)

1. Проверить что не осталось хардкода
2. Удалить backup файлы
3. Запустить линтер
4. Визуально проверить UI
5. Создать migration summary

---

## 🛠️ Инструменты и скрипты

### Скрипт поиска хардкода:

```bash
#!/bin/bash
# scripts/find-hardcoded-colors.sh

echo "Поиск хардкод цветов в проекте..."
echo "=================================="
echo ""

# Найти все hex цвета
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" --include="*.ts" \
  --exclude="*.test.ts" \
  --exclude="*.spec.ts" \
  -n | grep -v "tokens.colors" | wc -l

echo ""
echo "Топ файлов с хардкодом:"
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" --include="*.ts" \
  --exclude="*.test.ts" \
  -h | grep -v "tokens.colors" | \
  cut -d: -f1 | sort | uniq -c | sort -rn | head -10
```

### Скрипт автозамены:

```bash
#!/bin/bash
# scripts/migrate-colors.sh

FILE=$1

if [ -z "$FILE" ]; then
  echo "Usage: ./migrate-colors.sh <file>"
  exit 1
fi

echo "Миграция цветов в $FILE..."

# Backup
cp "$FILE" "$FILE.backup"

# Добавить import если нет
if ! grep -q "import.*tokens.*from.*ui" "$FILE"; then
  # Добавить после последнего import
  sed -i "/^import/a import { tokens } from '../ui';" "$FILE"
fi

# Основные замены
sed -i "s/'#4F46E5'/tokens.colors.primary.main/g" "$FILE"
sed -i "s/'#111827'/tokens.colors.text.primary/g" "$FILE"
sed -i "s/'#6B7280'/tokens.colors.text.secondary/g" "$FILE"
sed -i "s/'#F9FAFB'/tokens.colors.gray[50]/g" "$FILE"
sed -i "s/'#F3F4F6'/tokens.colors.gray[100]/g" "$FILE"
sed -i "s/'#E5E7EB'/tokens.colors.border/g" "$FILE"
sed -i "s/'#FFFFFF'/tokens.colors.surface/g" "$FILE"
sed -i "s/'#10B981'/tokens.colors.success.main/g" "$FILE"
sed -i "s/'#F59E0B'/tokens.colors.warning.main/g" "$FILE"
sed -i "s/'#DC2626'/tokens.colors.error.main/g" "$FILE"

echo "Готово! Проверьте результат."
echo "Backup: $FILE.backup"
```

---

## ✅ Чеклист для каждого файла

### Перед миграцией:
- [ ] Создать backup файла
- [ ] Проверить текущее состояние (скриншот UI)
- [ ] Запустить `npm run typecheck` (убедиться что нет ошибок)

### Во время миграции:
- [ ] Добавить import tokens
- [ ] Заменить частые цвета (автоматически)
- [ ] Заменить специальные цвета (вручную)
- [ ] Проверить TypeScript: `npm run typecheck`
- [ ] Проверить линтер

### После миграции:
- [ ] Визуально проверить UI (сравнить с backup скриншотом)
- [ ] Тестировать функциональность
- [ ] Удалить backup если всё ок
- [ ] Коммит изменений

---

## 📊 Метрики прогресса

### Отслеживание:

```bash
# Сколько осталось хардкода
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" -h | \
  grep -v "tokens.colors" | wc -l

# Прогресс
echo "Осталось: <число> из 509 (XX%)"
```

### Целевые метрики:

- **Старт:** 509 хардкодов
- **Цель:** <50 хардкодов (90% миграция)
- **Идеал:** <10 хардкодов (98% миграция)

---

## 🎯 Приоритеты

### 🔴 Высокий приоритет (сделать в первую очередь):
1. `ScheduleScreen.tsx` - самый используемый экран
2. `Dashboard/` экраны - админ панель

### 🟡 Средний приоритет:
3. Другие admin экраны
4. Employee экраны

### 🟢 Низкий приоритет:
5. Утилиты
6. Helpers
7. Конфиги

---

## ⚠️ Потенциальные проблемы

### 1. Динамические цвета

```tsx
// Проблема
const color = isActive ? '#10B981' : '#DC2626';

// Решение
const color = isActive 
  ? tokens.colors.success.main 
  : tokens.colors.error.main;
```

### 2. Interpolation в строках

```tsx
// Проблема
backgroundColor: `#${someVar}`;

// Решение - оставить как есть или рефакторить
```

### 3. Цвета в массивах/объектах

```tsx
// Проблема
const colors = ['#10B981', '#F59E0B', '#DC2626'];

// Решение
const colors = [
  tokens.colors.success.main,
  tokens.colors.warning.main,
  tokens.colors.error.main,
];
```

---

## 📝 Шаблон коммита

```
refactor(colors): migrate <file> from hardcoded to tokens

- Replace ~XX hardcoded hex colors with tokens.colors.*
- Add tokens import
- Verify visual consistency

Part of color migration initiative (#509 total)
```

---

## 🎉 Критерии успеха

### Минимум (90%):
- ✅ ScheduleScreen.tsx мигрирован
- ✅ Dashboard экраны мигрированы
- ✅ <50 хардкодов осталось
- ✅ 0 визуальных регрессий
- ✅ 0 TypeScript ошибок

### Идеал (98%):
- ✅ Все .tsx файлы мигрированы
- ✅ <10 хардкодов осталось (только обоснованные)
- ✅ Документация обновлена
- ✅ Migration summary создан

---

## 📚 Полезные команды

```bash
# Найти все хардкод цвета
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" | grep -v "tokens.colors"

# Посчитать хардкоды в файле
grep -o "#[0-9A-F]\{6\}" src/employee/ScheduleScreen.tsx | wc -l

# Найти уникальные цвета
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" -oh | sort | uniq

# Проверка после миграции
npm run typecheck && echo "✅ TypeScript OK"

# Найти файлы с импортом tokens
grep -r "import.*tokens" src/ --include="*.tsx"
```

---

## 🔄 Workflow для следующей сессии

### Начало сессии:

1. **Проверить текущее состояние:**
```bash
npm run typecheck
npm start  # визуально проверить что работает
```

2. **Запустить скрипт поиска:**
```bash
./scripts/find-hardcoded-colors.sh
```

3. **Выбрать файл для миграции** (начать с ScheduleScreen.tsx)

4. **Создать backup и начать миграцию**

### Во время миграции:

1. Мигрировать один файл за раз
2. После каждого: `npm run typecheck`
3. Визуально проверять UI
4. Коммитить по файлам

### Конец сессии:

1. Подсчитать прогресс
2. Обновить MIGRATION_PLAN.md
3. Создать checkpoint

---

## 📄 Связанные документы

- [`COLOR_PALETTE.md`](./COLOR_PALETTE.md) - карта замены
- [`COLOR_AUDIT.md`](./COLOR_AUDIT.md) - обоснование
- [`COLOR_OPTIMIZATION_REPORT.md`](./COLOR_OPTIMIZATION_REPORT.md) - отчёт
- [`colors.deprecated.ts`](../src/ui/theme/colors.deprecated.ts) - устаревшие цвета

---

**Готов к миграции!** 🚀

**Следующая сессия:** Начнём с ScheduleScreen.tsx (100+ хардкодов)

