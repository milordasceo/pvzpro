# 🎨 Полная цветовая палитра приложения WB ПВЗ

**Дата создания:** 20 октября 2025  
**Версия:** 1.0

---

## 📋 Содержание

1. [Основная палитра](#основная-палитра)
2. [Использование в коде](#использование-в-коде)
3. [Миграция с хардкода](#миграция-с-хардкода)
4. [Статусные цвета](#статусные-цвета)
5. [Специальные цвета](#специальные-цвета)

---

## 🎨 Основная палитра

### Primary (Indigo/Violet) - Основной бренд

Используется для:
- Активные элементы
- Кнопки действий
- Акценты
- Выделение активного состояния

```tsx
// Импорт
import { tokens } from '../ui';

// Использование
tokens.colors.primary.main      // '#4F46E5' - основной
tokens.colors.primary[50]        // '#F5F3FF' - очень светлый
tokens.colors.primary[100]       // '#EDE9FE'
tokens.colors.primary[200]       // '#DDD6FE'
tokens.colors.primary[300]       // '#C4B5FD'
tokens.colors.primary[400]       // '#A78BFA'
tokens.colors.primary[500]       // '#8B5CF6'
tokens.colors.primary[600]       // '#7C3AED' - secondary
tokens.colors.primary[700]       // '#6D28D9'
tokens.colors.primary[800]       // '#5B21B6'
tokens.colors.primary[900]       // '#4C1D95' - очень тёмный
```

**Визуальные примеры:**
- 🟣 Фиолетовый - основной цвет кнопок
- 🟣 Индиго - акценты и выделения
- 💜 Светлый - фоны активных элементов

---

### Gray (Нейтральные) - Основа интерфейса

Используется для:
- Текст (разные уровни)
- Фоны
- Границы
- Разделители
- Неактивные элементы

```tsx
tokens.colors.gray[50]           // '#F9FAFB' - фон экрана
tokens.colors.gray[100]          // '#F3F4F6' - фон секций
tokens.colors.gray[200]          // '#E5E7EB' - границы
tokens.colors.gray[300]          // '#D1D5DB' - disabled элементы
tokens.colors.gray[400]          // '#9CA3AF' - muted текст
tokens.colors.gray[500]          // '#6B7280' - вторичный текст
tokens.colors.gray[600]          // '#4B5563' - тёмный текст
tokens.colors.gray[700]          // '#374151' - очень тёмный
tokens.colors.gray[800]          // '#1F2937' - почти чёрный
tokens.colors.gray[900]          // '#111827' - основной текст
```

**Визуальная иерархия:**
```
gray[900] ████████████ Заголовки, основной текст
gray[700] ███████████░ Подзаголовки
gray[500] ████████░░░░ Вторичный текст, метаданные
gray[400] ██████░░░░░░ Placeholder, muted текст
gray[300] ████░░░░░░░░ Disabled элементы
gray[200] ██░░░░░░░░░░ Границы, разделители
gray[100] █░░░░░░░░░░░ Фон секций
gray[50]  ░░░░░░░░░░░░ Фон экрана
```

---

### Success (Зелёный) - Успех

Используется для:
- Успешные операции
- Активный статус
- На смене
- Подтверждения
- Положительные показатели

```tsx
tokens.colors.success.light      // '#D1FAE5' - светлый фон
tokens.colors.success.main       // '#10B981' - основной
tokens.colors.success.dark       // '#059669' - тёмный текст
```

**Примеры:**
- ✅ "На смене" бейдж
- ✅ Успешное завершение действия
- ✅ Положительная статистика
- ✅ Подтверждение выполнения

**Дополнительные оттенки (используются в коде):**
```tsx
'#ECFDF5' // Очень светлый фон для карточек
'#16A34A' // green-600 для границ
'#EF4444' // red-500 (для контраста с success)
```

---

### Warning (Жёлтый/Оранжевый) - Предупреждение

Используется для:
- Предупреждения
- Опоздания
- Требует внимания
- Промежуточные состояния

```tsx
tokens.colors.warning.light      // '#FEF3C7' - светлый фон
tokens.colors.warning.main       // '#F59E0B' - основной
tokens.colors.warning.dark       // '#D97706' - тёмный текст
```

**Примеры:**
- ⚠️ "Опоздание" бейдж
- ⚠️ Предупреждения системы
- ⚠️ Незавершённые задачи
- ⚠️ Требует проверки

**Дополнительные оттенки:**
```tsx
'#92400E' // amber-900 для текста на светлом фоне
```

---

### Error (Красный) - Ошибка

Используется для:
- Ошибки
- Отклонённые запросы
- Критичные уведомления
- Удаление

```tsx
tokens.colors.error.light        // '#FEE2E2' - светлый фон
tokens.colors.error.main         // '#DC2626' - основной
tokens.colors.error.dark         // '#B91C1C' - тёмный текст
```

**Примеры:**
- ❌ Ошибки валидации
- ❌ Отклонённые запросы
- ❌ Критичные уведомления
- ❌ Кнопки удаления

**Дополнительные оттенки:**
```tsx
'#FEF2F2' // red-50 для светлого фона
'#991B1B' // red-800 для текста
```

---

### Info (Cyan/Blue) - Информация

Используется для:
- Информационные сообщения
- Нейтральные уведомления
- Дополнительная информация

```tsx
tokens.colors.info.light         // '#CFFAFE' - светлый фон
tokens.colors.info.main          // '#0891B2' - основной
tokens.colors.info.dark          // '#0E7490' - тёмный текст
```

**Примеры:**
- ℹ️ Информационные подсказки
- ℹ️ Нейтральные уведомления
- ℹ️ Справочная информация

**Дополнительные оттенки:**
```tsx
'#EFF6FF' // blue-50 для светлого фона
'#BFDBFE' // blue-200 для границ
'#1E40AF' // blue-800 для текста
```

---

## 🎯 Использование в коде

### ✅ Правильно (через tokens):

```tsx
import { tokens } from '../ui';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: tokens.colors.background,
  },
  
  card: {
    backgroundColor: tokens.colors.surface,
    borderColor: tokens.colors.border,
  },
  
  title: {
    color: tokens.colors.text.primary,
  },
  
  subtitle: {
    color: tokens.colors.text.secondary,
  },
  
  successButton: {
    backgroundColor: tokens.colors.success.main,
  },
  
  errorText: {
    color: tokens.colors.error.main,
  },
});
```

### ❌ Неправильно (хардкод):

```tsx
// НЕ делай так!
const styles = StyleSheet.create({
  container: {
    backgroundColor: '#F9FAFB',  // ❌
  },
  
  title: {
    color: '#111827',  // ❌
  },
});
```

---

## 🔄 Миграция с хардкода

### Найденные хардкод цвета в проекте:

#### Часто используемые:

| Хардкод | Замена | Назначение |
|---------|--------|------------|
| `#4F46E5` | `tokens.colors.primary.main` | Primary цвет |
| `#111827` | `tokens.colors.text.primary` | Основной текст |
| `#6B7280` | `tokens.colors.text.secondary` | Вторичный текст |
| `#9CA3AF` | `tokens.colors.text.muted` | Приглушённый текст |
| `#F9FAFB` | `tokens.colors.gray[50]` | Фон экрана |
| `#F3F4F6` | `tokens.colors.gray[100]` | Фон секций |
| `#E5E7EB` | `tokens.colors.border` | Границы |
| `#FFFFFF` | `tokens.colors.surface` | Белый фон |
| `#10B981` | `tokens.colors.success.main` | Успех |
| `#F59E0B` | `tokens.colors.warning.main` | Предупреждение |
| `#DC2626` | `tokens.colors.error.main` | Ошибка |
| `#0891B2` | `tokens.colors.info.main` | Информация |

#### Специальные оттенки:

| Хардкод | Замена | Назначение |
|---------|--------|------------|
| `#D1FAE5` | `tokens.colors.success.light` | Светлый фон успеха |
| `#FEE2E2` | `tokens.colors.error.light` | Светлый фон ошибки |
| `#FEF3C7` | `tokens.colors.warning.light` | Светлый фон предупреждения |
| `#CFFAFE` | `tokens.colors.info.light` | Светлый фон инфо |
| `#059669` | `tokens.colors.success.dark` | Тёмный текст успеха |
| `#D97706` | `tokens.colors.warning.dark` | Тёмный текст предупреждения |
| `#B91C1C` | `tokens.colors.error.dark` | Тёмный текст ошибки |

#### Дополнительные (нужно добавить в tokens):

| Хардкод | Предлагаемое место | Назначение |
|---------|-------------------|------------|
| `#ECFDF5` | `tokens.colors.success.lighter` | Очень светлый зелёный |
| `#FEF2F2` | `tokens.colors.error.lighter` | Очень светлый красный |
| `#EFF6FF` | `tokens.colors.info.lighter` | Очень светлый синий |
| `#16A34A` | `tokens.colors.success[600]` | Зелёный для границ |
| `#EF4444` | `tokens.colors.error[500]` | Красный базовый |
| `#1E40AF` | `tokens.colors.info[800]` | Синий для текста |
| `#92400E` | `tokens.colors.warning[900]` | Тёмно-коричневый |
| `#991B1B` | `tokens.colors.error[800]` | Тёмно-красный |

---

## 🔧 План миграции

### Этап 1: Расширить палитру (если нужно)

Добавить недостающие оттенки в `src/ui/theme/colors.ts`:

```tsx
export const colors = {
  // ... существующие
  
  success: {
    lighter: '#ECFDF5',  // Добавить
    light: '#D1FAE5',
    main: '#10B981',
    600: '#16A34A',      // Добавить
    dark: '#059669',
  },
  
  error: {
    lighter: '#FEF2F2',  // Добавить
    light: '#FEE2E2',
    500: '#EF4444',      // Добавить
    main: '#DC2626',
    800: '#991B1B',      // Добавить
    dark: '#B91C1C',
  },
  
  warning: {
    light: '#FEF3C7',
    main: '#F59E0B',
    900: '#92400E',      // Добавить
    dark: '#D97706',
  },
  
  info: {
    lighter: '#EFF6FF',  // Добавить
    200: '#BFDBFE',      // Добавить
    light: '#CFFAFE',
    main: '#0891B2',
    800: '#1E40AF',      // Добавить
    dark: '#0E7490',
  },
};
```

### Этап 2: Обновить файлы с хардкодом

**Файлы требующие обновления:**

1. ✅ `src/theme.ts` - уже обновлён
2. ✅ `src/components/StyledCard.tsx` - уже обновлён
3. ✅ `src/navigation/AdminTabNavigator.tsx` - частично обновлён
4. ⚠️ `src/employee/ScheduleScreen.tsx` - **509 хардкод цветов!**
5. ⚠️ `src/admin/screens/dashboard/StatisticsTab.tsx` - много хардкода
6. ⚠️ `src/admin/screens/dashboard/OnShiftTab.tsx` - много хардкода
7. ⚠️ Другие файлы с меньшим количеством

### Этап 3: Проверка

```bash
# Найти все хардкод цвета
grep -r "#[0-9A-F]\{6\}" src/ --include="*.tsx" --include="*.ts"

# После миграции должно быть значительно меньше результатов
```

---

## 🎨 Семантические цвета

### Фоны:

```tsx
tokens.colors.background         // '#FBFCFE' - основной фон приложения
tokens.colors.surface            // '#FFFFFF' - фон карточек
tokens.colors.gray[50]           // '#F9FAFB' - альтернативный фон
tokens.colors.gray[100]          // '#F3F4F6' - фон секций
```

### Границы и разделители:

```tsx
tokens.colors.border             // '#E5E7EB' - основные границы
tokens.colors.divider            // '#F3F4F6' - разделители
tokens.colors.gray[200]          // '#E5E7EB' - то же что border
```

### Текст:

```tsx
tokens.colors.text.primary       // '#111827' - основной текст
tokens.colors.text.secondary     // '#6B7280' - вторичный текст
tokens.colors.text.muted         // '#9CA3AF' - приглушённый
tokens.colors.text.disabled      // '#D1D5DB' - отключённый
tokens.colors.text.white         // '#FFFFFF' - белый текст
```

### Overlay (для модалов):

```tsx
tokens.colors.overlay            // 'rgba(0, 0, 0, 0.5)' - тёмный overlay
tokens.colors.backdrop           // 'rgba(0, 0, 0, 0.25)' - светлый backdrop
```

---

## 📊 Статистика использования

### Текущее состояние (найдено grep):
- **Всего хардкод цветов:** ~509 вхождений
- **Больше всего в:** `ScheduleScreen.tsx` (>100)
- **Миграция завершена:** ~10% файлов

### План действий:
1. ✅ Создать полную палитру
2. ⚠️ Расширить недостающими оттенками
3. ⚠️ Мигрировать ScheduleScreen.tsx
4. ⚠️ Мигрировать Dashboard экраны
5. ⚠️ Мигрировать остальные файлы

---

## 🎯 Рекомендации

### Для новых компонентов:
1. **ВСЕГДА** используй `tokens.colors.*`
2. **НИКОГДА** не используй хардкод цвета
3. Проверяй через UI Catalog (тройной тап на FAB)

### Для рефакторинга:
1. Начни с самых используемых файлов
2. Используй поиск и замену
3. Проверяй визуально после каждого файла
4. Проверяй TypeScript: `npm run typecheck`

### Best Practices:
```tsx
// ✅ Правильно
backgroundColor: tokens.colors.success.light,
color: tokens.colors.text.primary,
borderColor: tokens.colors.border,

// ❌ Неправильно
backgroundColor: '#D1FAE5',
color: '#111827',
borderColor: '#E5E7EB',
```

---

## 📚 Связанные документы

- [`UI_TOKENS.md`](./UI_TOKENS.md) - Все design tokens
- [`UI_COMPONENTS.md`](./UI_COMPONENTS.md) - Каталог компонентов
- [`UI_GUIDELINES.md`](./UI_GUIDELINES.md) - Правила использования
- [`src/ui/theme/colors.ts`](../src/ui/theme/colors.ts) - Реализация палитры

---

## 🔍 Быстрая справка

**Посмотреть все цвета визуально:**
1. Тройной тап на FAB кнопку (палитра в углу)
2. Вкладка "Цвета"
3. Все цвета с hex кодами!

**Найти хардкод в файле:**
```bash
grep "#[0-9A-F]\{6\}" filename.tsx
```

**Заменить автоматически (пример):**
```bash
# Замена одного цвета во всех файлах
find src -name "*.tsx" -exec sed -i "s/#4F46E5/tokens.colors.primary.main/g" {} +
```

---

**Единая цветовая палитра = Консистентный дизайн!** 🎨

