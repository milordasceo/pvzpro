# 🛠️ Dev Tools - Инструменты разработчика

**Вспомогательные инструменты для ускорения разработки**

---

## 🎨 UI Catalog FAB

**Плавающая кнопка для быстрого доступа к UI Catalog**

### Расположение

Правый нижний угол экрана (на всех экранах приложения)

### Как использовать

1. **Тройной тап** на кнопку с иконкой палитры 🎨
2. Откроется UI Catalog Screen
3. Просмотрите компоненты, цвета, отступы

### Особенности

- ✅ Только в DEV режиме (`__DEV__`)
- ✅ Можно перетаскивать в удобное место
- ✅ Полупрозрачная, не мешает работе
- ✅ Показывает индикатор при тапах
- ✅ Автоматически скрывается в production

### Документация

📄 [`DevUICatalogFAB.README.md`](../src/components/DevUICatalogFAB.README.md)

---

## 🎨 UI Catalog Screen

**Визуальный каталог всей UI системы приложения**

### Доступ

**Способ 1:** Тройной тап на FAB кнопку (⚡ быстро)  
**Способ 2:** Админ панель → Таб "UI"

### Содержимое

#### Вкладка "Цвета" 🎨

- Primary colors (10 оттенков)
- Gray colors (10 оттенков)
- Status colors (Success, Warning, Error, Info)
- Semantic colors (Background, Surface, Border)
- Text colors (Primary, Secondary, Muted, Disabled)
- Badge colors
- **Все с hex кодами!**

#### Вкладка "Отступы" 📏

- Spacing scale (xs, sm, md, lg, xl, xxl, xxxl)
- Border radius (none, xs, sm, md, lg, xl, xxl, full)
- Component heights (Control, Button, Input, TabBar, Header)
- **Все с размерами в пикселях!**

#### Вкладка "Типографика" ✏️

- Paper Text variants (Display, Headline, Title, Body, Label)
- Font sizes (xs → huge)
- Font weights (Regular, Medium, Semibold, Bold)
- **Все с примерами!**

#### Вкладка "Компоненты" 🧩

- SearchInput (с примером)
- StatusBadge (все типы и размеры)
- EmptyState, LoadingState, ErrorState
- Paper компоненты (Badge, Chip, Avatar, ActivityIndicator)
- StyledButton, SquareIconButton
- StyledCard, Surface
- **Все с интерактивными примерами!**

### Использование

1. Выберите нужную вкладку
2. Найдите элемент
3. Скопируйте код/значение
4. Используйте в своём коде

---

## 🔍 TypeScript проверка

### Команды

```bash
# Проверка типов
npm run typecheck

# Непрерывная проверка (watch mode)
npx tsc --noEmit --watch
```

### В CI/CD

Автоматическая проверка при каждом коммите

---

## 📝 Линтер

### Проверка конкретных файлов

В редакторе автоматически подсвечиваются ошибки

### Исправление

Большинство ошибок исправляются автоматически при сохранении

---

## 🚀 Быстрый запуск

### Основная команда

```bash
START.bat
```

Автоматически:

- Останавливает предыдущие процессы
- Очищает кэш Metro
- Запускает Expo с туннелем
- Открывает в браузере

### Без туннеля

```bash
start-without-tunnel.bat
```

Для локальной разработки (быстрее)

---

## 📱 Подключение устройств

### Android (физическое устройство)

```bash
scrcpy_start.bat
```

Запускает scrcpy для отображения экрана Android

### Любое устройство

Сканируйте QR код в Expo Go

---

## 🔧 Отладка

### React DevTools

```bash
npx react-devtools
```

### Flipper

Настроен для React Native Paper

### Chrome DevTools

Встроенные в Expo

---

## 📊 Мониторинг производительности

### React Native Performance

- Встроен в DEV меню
- Shake gesture → "Perf Monitor"

### Hermes

Оптимизированный JS engine для production

---

## 🎯 Mock данные

### Расположение

`src/admin/services/mockData.ts`

### Доступно

- `MOCK_EMPLOYEES` - 12 сотрудников (4 ПВЗ)
- `MOCK_SHIFTS` - История смен (7 дней)
- `MOCK_PVZ` - 4 точки выдачи

### Использование

```tsx
import { MOCK_EMPLOYEES, MOCK_PVZ } from '../admin/services/mockData';

// В компоненте
const [employees] = useState(MOCK_EMPLOYEES);
```

---

## 📚 Документация

### Основные документы

- [`UI_TOKENS.md`](./UI_TOKENS.md) - Design Tokens
- [`UI_COMPONENTS.md`](./UI_COMPONENTS.md) - Каталог компонентов
- [`UI_GUIDELINES.md`](./UI_GUIDELINES.md) - Правила использования
- [`NEXT_SESSION.md`](./NEXT_SESSION.md) - План развития

### README файлы

- [`AnimatedTabBar.README.md`](../src/components/AnimatedTabBar.README.md)
- [`DevUICatalogFAB.README.md`](../src/components/DevUICatalogFAB.README.md)

---

## 🔑 Горячие клавиши

### Metro Bundler

- `r` - Reload
- `d` - Dev Menu
- `i` - iOS simulator
- `a` - Android emulator
- `w` - Web browser
- `j` - Debugger

### В приложении (Shake или Cmd+D / Ctrl+M)

- **Reload** - Перезагрузить
- **Debug** - Chrome DevTools
- **Perf Monitor** - Монитор производительности
- **Toggle Inspector** - Инспектор элементов
- **Show Dev Menu** - Показать меню

---

## 🎨 Быстрый доступ к компонентам

### Через FAB

1. Тройной тап на кнопку палитры
2. Выбрать вкладку
3. Найти компонент

### Через код

```tsx
import { tokens, SearchInput, StatusBadge } from '../ui';
import { StyledButton, StyledCard } from '../components';
```

### Через документацию

1. Открыть `UI_COMPONENTS.md`
2. Найти нужный компонент
3. Скопировать пример

---

## 🐛 Отладка UI

### 1. Визуальная проверка

- Открыть UI Catalog
- Сравнить с примером
- Проверить токены

### 2. Инспектор элементов

- Dev Menu → Toggle Inspector
- Тапнуть на элемент
- Посмотреть стили

### 3. Console.log

```tsx
console.log('tokens:', tokens.colors.primary.main);
console.log('component:', { visible, loading });
```

### 4. React DevTools

- Просмотр дерева компонентов
- Редактирование props
- Профилирование рендеров

---

## ⚡ Советы по производительности

### 1. Используйте UI Catalog

Все компоненты уже оптимизированы и мемоизированы

### 2. Проверяйте рендеры

```tsx
// Включить профайлер
React.Profiler + React DevTools
```

### 3. Избегайте

- Inline стили
- Анонимные функции в render
- Ненужные re-renders

### 4. Используйте

- `React.memo`
- `useMemo` / `useCallback`
- `StyleSheet.create`

---

## 📦 Работа с зависимостями

### Обновление

```bash
npm outdated
npm update
```

### Очистка

```bash
# Очистка node_modules
rm -rf node_modules
npm install

# Очистка Metro cache
npm start -- --clear
```

---

## 🔗 Полезные ссылки

### Внутренние

- [UI Catalog Screen](../src/admin/screens/UICatalogScreen.tsx)
- [Dev FAB Component](../src/components/DevUICatalogFAB.tsx)
- [Theme System](../src/ui/theme/)

### Внешние

- [React Native Paper Docs](https://callstack.github.io/react-native-paper/)
- [Expo Docs](https://docs.expo.dev/)
- [React Navigation](https://reactnavigation.org/)

---

**Эффективная разработка с правильными инструментами!** 🚀
