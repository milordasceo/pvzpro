# Дизайн-система и Токены (Design Tokens)

## 📌 Основная концепция
В проекте реализована строгая дизайн-система, основанная на **дизайн-токенах**. Это гарантирует консистентность интерфейса, масштабируемость и легкость кастомизации (например, смену темы маркетплейса: WB, Ozon, Яндекс).

**Золотое правило:** Запрещено использовать хардкод-значения для цветов, шрифтов, отступов и скруглений. Все значения должны браться из объекта `theme`.

---

## 🏗️ Структура токенов (`src/shared/theme/`)

### 1. Типографика (`tokens.ts`)
Мы используем семантические пресеты. Вместо указания `fontSize: 16`, используйте пресет, который подходит по смыслу:

| Пресет | Применение | Параметры (размер/вес) |
| :--- | :--- | :--- |
| `h1` | Главные заголовки экранов | 32px / Black |
| `h2` | Заголовки разделов | 24px / Black |
| `h3` | Заголовки карточек | 18px / Bold |
| `h4` | Малые заголовки | 16px / Bold |
| `body` | Основной текст (информативный) | 15px / Semibold |
| `bodySmall` | Вспомогательный текст | 13px / Medium |
| `caption` | Технический текст (верхний регистр) | 11px / Black |
| `label` | Подписи, теги | 11px / Bold |

**Важно для доступности (40+):** Минимальный размер шрифта для чтения — **11px**. Основной текст не должен быть меньше **15px**.

### 2. Цвета (`colors.ts`)
Цвета делятся на брендовые и семантические:
- `theme.colors.primary` — основной цвет текущего маркетплейса.
- `theme.colors.surface.primary` — фон карточек/контейнеров.
- `theme.colors.text.primary` — основной цвет текста.
- `theme.colors.border.DEFAULT` — стандартный цвет границ.

### 3. Отступы и Скругления (`tokens.ts` & `layout.ts`)
Используйте сетку на основе 4px/8px:
- `theme.spacing.sm` (8px), `md` (12px), `lg` (16px), `xl` (20px).
- `theme.borderRadius.xl` (14px), `theme.borderRadius['3xl']` (20px).

---

## 🛠️ Как использовать в коде

### В стилях (StyleSheet)
```tsx
import { theme } from '../../../shared/theme';

const styles = StyleSheet.create({
  card: {
    padding: theme.spacing.lg,
    borderRadius: theme.borderRadius.xl,
    backgroundColor: theme.colors.surface.primary,
  },
  title: {
    ...theme.typography.presets.h3,
    color: theme.colors.text.primary,
  }
});
```

### С иконками (Lucide)
```tsx
<Camera 
  size={theme.layout.icon.lg} 
  color={theme.colors.primary} 
/>
```

---

## 📝 Правила внесения изменений
1. **Никакого хардкода.** Если нужного значения нет в токенах, добавьте его в `tokens.ts`.
2. **Семантика важнее внешнего вида.** Выбирайте пресет шрифта по его роли (заголовок, тело, подпись), а не просто потому что "размер похож".
3. **Проверка на доступность.** При изменении размеров шрифтов или контрастности, учитывайте пользователей старшего возраста (крупные тач-зоны, читаемый текст).
4. **Темы маркетплейсов.** При добавлении новых UI-элементов убедитесь, что они корректно окрашиваются через `theme.colors.primary`, который меняется динамически.

---
**Последнее обновление:** 21 декабря 2025  
**Статус:** ✅ Обязательно к исполнению

