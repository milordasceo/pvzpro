# 🏗️ Архитектура проекта WB ПВЗ

> Документация архитектуры мобильного приложения для управления пунктами выдачи заказов Wildberries

**Версия**: 2.1.0  
**Дата**: 04 ноября 2025 - 19:10

---

## 📋 Содержание

- [Общий обзор](#общий-обзор)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Основные модули](#основные-модули)
- [Потоки данных](#потоки-данных)
- [Архитектурные решения](#архитектурные-решения)
- [Зависимости между модулями](#зависимости-между-модулями)

---

## Общий обзор

WB ПВЗ - это кроссплатформенное мобильное приложение на React Native + Expo для управления пунктами выдачи заказов Wildberries. Приложение предназначено для трёх типов пользователей:

- **Owner** - владелец сети ПВЗ (управление администраторами и ПВЗ)
- **Admin** - администратор ПВЗ (управление сотрудниками, сменами, контроль работы)
- **Employee** - сотрудник ПВЗ (выполнение операций, просмотр графика)

### Архитектурные принципы

1. **UI-First подход** - сначала создаётся полный UI/UX, затем интегрируется бэкенд
2. **Component-based architecture** - переиспользуемые компоненты на базе React Native Paper
3. **Type-safe** - строгая типизация TypeScript на всех уровнях
4. **Minimal state management** - Zustand для глобального состояния, React hooks для локального
5. **Mock-first development** - разработка с mock данными для независимости от бэкенда

---

## Технологический стек

### Core
- **React** 19.1.0 - UI библиотека
- **React Native** 0.81.5 - мобильная платформа
- **Expo SDK** 54.0.22 - инструментарий для разработки
- **TypeScript** 5.9.3 - строгая типизация (strict mode)

### UI/UX
- **React Native Paper** 5.14.5 - Material Design 3 компоненты
- **React Navigation** 7 - навигация (Native Stack, Bottom Tabs, Material Top Tabs)
- **@expo/vector-icons** - иконки

### State Management
- **Zustand** 5.0.8 - глобальное состояние приложения
- **AsyncStorage** 2.2.0 - персистентное хранилище

### Dev Tools
- **ESLint** 9.39.1 - линтинг кода (Flat Config)
- **Prettier** 3.6.2 - форматирование кода
- **TypeScript ESLint** 8.46.2 - типизированный линтинг

### Additional
- **Expo Camera** - работа с камерой (QR коды)
- **Expo Location** - геолокация
- **Expo Notifications** - push-уведомления
- **React Native Maps** - карты для отображения ПВЗ

---

## Структура проекта

```
wbapp/
├── src/                          # Исходный код приложения
│   ├── components/               # Переиспользуемые компоненты
│   │   ├── common/              # Общие компоненты (Button, Card, Input)
│   │   ├── layout/              # Layout компоненты (Header, Container)
│   │   └── domain/              # Доменные компоненты (EmployeeCard, OrderCard)
│   │
│   ├── screens/                  # Экраны приложения
│   │   ├── auth/                # Аутентификация (Login, Register)
│   │   ├── admin/               # Экраны администратора
│   │   ├── employee/            # Экраны сотрудника
│   │   └── owner/               # Экраны владельца
│   │
│   ├── navigation/               # Навигация
│   │   ├── RootNavigator.tsx    # Главный навигатор
│   │   ├── AdminNavigator.tsx   # Навигатор администратора
│   │   ├── EmployeeNavigator.tsx # Навигатор сотрудника
│   │   └── types.ts             # Типы навигации
│   │
│   ├── store/                    # Zustand stores
│   │   ├── authStore.ts         # Состояние аутентификации
│   │   ├── employeeStore.ts     # Состояние сотрудников
│   │   └── notificationStore.ts # Состояние уведомлений
│   │
│   ├── services/                 # Сервисы и API
│   │   ├── api/                 # API клиенты
│   │   ├── storage/             # AsyncStorage обёртки
│   │   └── notifications/       # Работа с уведомлениями
│   │
│   ├── hooks/                    # Custom React hooks
│   │   ├── useAuth.ts
│   │   ├── useEmployees.ts
│   │   └── useTheme.ts
│   │
│   ├── utils/                    # Утилиты и хелперы
│   │   ├── formatters.ts        # Форматирование данных
│   │   ├── validators.ts        # Валидация
│   │   └── constants.ts         # Константы
│   │
│   ├── types/                    # TypeScript типы и интерфейсы
│   │   ├── models/              # Модели данных (User, Employee, Order)
│   │   └── api/                 # API типы
│   │
│   └── theme/                    # Дизайн-система
│       ├── tokens.ts            # Design tokens
│       ├── colors.ts            # Цветовая палитра
│       └── typography.ts        # Типографика
│
├── docs/                         # Документация
├── assets/                       # Статические ресурсы (изображения, шрифты)
├── scripts/                      # Скрипты автоматизации
├── android/                      # Нативный код Android
├── ios/                          # Нативный код iOS (будет добавлено)
│
├── .cursorrules                  # Правила для AI IDE
├── package.json                  # Зависимости и скрипты
├── tsconfig.json                 # TypeScript конфигурация
├── eslint.config.js              # ESLint конфигурация
├── app.config.ts                 # Expo конфигурация
└── README.md                     # Главный README
```

---

## Основные модули

### 1. Модуль Аутентификации

**Назначение**: Управление аутентификацией и авторизацией пользователей

**Компоненты**:
- `LoginScreen` - экран входа
- `RegisterScreen` - экран регистрации (будущее)
- `authStore` - хранилище состояния авторизации

**Ответственность**:
- Аутентификация пользователей
- Хранение токенов и сессий
- Определение роли пользователя (Owner/Admin/Employee)
- Обработка logout

**Зависимости**:
- AsyncStorage для персистентности
- API сервис для запросов

### 2. Модуль Admin Dashboard

**Назначение**: Панель управления для администратора ПВЗ

**Компоненты**:
- `DashboardScreen` - главный экран с обзором
- `OnDutyScreen` - экран "На смене" с текущими сотрудниками
- Метрики и статистика

**Ответственность**:
- Отображение ключевых метрик
- Контроль текущих смен
- Быстрый доступ к критичным функциям

**Зависимости**:
- employeeStore
- API сервис для получения статистики

### 3. Модуль Сотрудники (в разработке)

**Назначение**: Управление сотрудниками ПВЗ

**Компоненты**:
- `EmployeesScreen` - список всех сотрудников
- `EmployeeProfileScreen` - профиль сотрудника
- `EmployeeCreateScreen` - создание нового сотрудника
- `EmployeeEditScreen` - редактирование сотрудника

**Ответственность**:
- CRUD операции с сотрудниками
- Управление статусами (активный/неактивный)
- История смен
- Статистика работы

**Зависимости**:
- employeeStore
- API сервис
- Navigation

**Статус**: 📋 План готов, в процессе реализации

Подробнее: [EMPLOYEES_MODULE_ARCHITECTURE_RU.md](docs/EMPLOYEES_MODULE_ARCHITECTURE_RU.md)

### 4. Модуль Навигации

**Назначение**: Управление навигацией между экранами

**Компоненты**:
- `RootNavigator` - главный навигатор с условным рендерингом
- `AdminNavigator` - Bottom Tabs навигатор для администратора
- `EmployeeNavigator` - навигатор для сотрудника

**Ответственность**:
- Роутинг между экранами
- Условная навигация по ролям
- Анимации переходов
- Deep linking (будущее)

**Зависимости**:
- React Navigation 7
- authStore (для определения роли)

### 5. Модуль UI Компонентов

**Назначение**: Библиотека переиспользуемых UI компонентов

**Компоненты**:
- Common: Button, Card, Input, Loading, EmptyState
- Layout: ScreenContainer, Header, TabBar
- Domain: EmployeeCard, OrderCard, MetricCard

**Ответственность**:
- Единый стиль компонентов
- Соответствие Material Design 3
- Переиспользуемость и композиция
- Типизация props

**Зависимости**:
- React Native Paper
- theme (design tokens)

---

## Потоки данных

### Архитектура данных

```
┌─────────────────┐
│   UI Screen     │ ← React Component
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Custom Hook    │ ← useEmployees(), useAuth()
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Zustand Store  │ ← Global State Management
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  API Service    │ ← HTTP Client (fetch/axios)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Backend API    │ ← REST API / GraphQL
└─────────────────┘
```

### Пример потока: Загрузка списка сотрудников

1. **UI Layer**: `EmployeesScreen` монтируется
2. **Hook Layer**: `useEmployees()` hook вызывается
3. **Store Layer**: `employeeStore.fetchEmployees()` запускается
4. **Service Layer**: `employeeApi.getEmployees()` делает HTTP запрос
5. **Backend**: Возвращает данные
6. **Store Layer**: Обновляет состояние
7. **UI Layer**: Перерендеривается с новыми данными

### State Management стратегия

**Zustand Stores** (глобальное состояние):
- Данные пользователя (authStore)
- Списки сотрудников (employeeStore)
- Уведомления (notificationStore)

**React State** (локальное состояние):
- Состояние форм
- UI состояние (открыто/закрыто, loading)
- Временные данные

**AsyncStorage** (персистентное):
- Токены аутентификации
- Настройки приложения
- Кэш данных (опционально)

---

## Архитектурные решения

### ADR-001: Использование React Native Paper

**Контекст**: Нужна UI библиотека для быстрой разработки с консистентным дизайном

**Решение**: Использовать React Native Paper 5 с Material Design 3

**Обоснование**:
- ✅ Готовые компоненты (Button, Card, TextInput, etc.)
- ✅ Material Design 3 - современный и знакомый пользователям
- ✅ Полная интеграция с React Navigation
- ✅ Поддержка тем и кастомизация
- ✅ TypeScript типизация

**Последствия**:
- Все UI компоненты должны базироваться на Paper
- Запрещено использовать другие UI библиотеки
- Необходимо следовать Material Design гайдлайнам

**Статус**: Принято ✅

---

### ADR-002: Zustand для State Management

**Контекст**: Нужен простой и производительный state manager

**Решение**: Использовать Zustand 5 вместо Redux/MobX

**Обоснование**:
- ✅ Минимальный boilerplate
- ✅ Отличная TypeScript интеграция
- ✅ Малый размер бандла (~1KB)
- ✅ Простота использования
- ✅ Не требует Provider обёртки

**Последствия**:
- Необходимо продумать структуру stores
- Используем middleware для persistence (AsyncStorage)
- DevTools через Redux DevTools

**Статус**: Принято ✅

---

### ADR-003: UI-First разработка

**Контекст**: Бэкенд разрабатывается параллельно с фронтендом

**Решение**: Разрабатывать сначала полный UI с mock данными, затем интегрировать API

**Обоснование**:
- ✅ Независимость от бэкенда
- ✅ Быстрая итерация по дизайну
- ✅ Возможность демонстрировать прогресс
- ✅ Раннее обнаружение UX проблем

**Последствия**:
- Необходимо создавать качественные mock данные
- Нужна чёткая граница между UI и API слоями
- Интеграция API должна быть простой заменой

**Статус**: Принято ✅

---

### ADR-004: Строгий TypeScript режим

**Контекст**: Необходимость высокого качества кода и минимизации багов

**Решение**: Использовать TypeScript в strict mode

**Обоснование**:
- ✅ Раннее обнаружение ошибок типов
- ✅ Лучшая поддержка IDE
- ✅ Самодокументируемый код
- ✅ Рефакторинг с уверенностью

**Последствия**:
- Все файлы должны быть типизированы
- Запрещён `any` (использовать `unknown` если нужно)
- Все API ответы должны иметь типы
- Strict null checks

**Статус**: Принято ✅

---

### ADR-005: Expo вместо Pure React Native

**Контекст**: Выбор между Expo и чистым React Native

**Решение**: Использовать Expo SDK 54

**Обоснование**:
- ✅ Быстрая разработка без нативного кода
- ✅ OTA updates через Expo
- ✅ Простая сборка (EAS Build)
- ✅ Большой набор готовых модулей
- ✅ Development client для расширений

**Последствия**:
- Зависимость от Expo SDK релизов
- Некоторые нативные модули недоступны напрямую
- Необходимо использовать expo-compatible библиотеки

**Статус**: Принято ✅

---

## Зависимости между модулями

### Граф зависимостей

```
┌──────────────┐
│ RootNavigator│
└──────┬───────┘
       │
       ├─→ AdminNavigator ──→ Admin Screens ─┐
       ├─→ EmployeeNavigator ──→ Employee Screens ─┤
       └─→ AuthNavigator ──→ Auth Screens ────────┤
                                                   │
                                                   ↓
                                            ┌──────────────┐
                                            │  Components  │
                                            └──────┬───────┘
                                                   │
                                            ┌──────↓───────┐
                                            │    Hooks     │
                                            └──────┬───────┘
                                                   │
                                            ┌──────↓───────┐
                                            │    Stores    │
                                            └──────┬───────┘
                                                   │
                                            ┌──────↓───────┐
                                            │   Services   │
                                            └──────────────┘
```

### Правила зависимостей

1. **Screens** могут использовать:
   - Components
   - Hooks
   - Navigation
   - Theme

2. **Components** могут использовать:
   - Другие Components (композиция)
   - Hooks (только presentational)
   - Theme
   - Utils

3. **Hooks** могут использовать:
   - Stores
   - Services
   - Utils
   - Другие hooks

4. **Stores** могут использовать:
   - Services
   - Utils

5. **Services** могут использовать:
   - Utils
   - Types

### Запрещённые зависимости

- ❌ Stores не должны импортировать Screens или Components
- ❌ Services не должны импортировать Stores (только используются Stores)
- ❌ Utils не должны иметь зависимостей (кроме других utils)
- ❌ Components не должны напрямую обращаться к Services (только через hooks)

---

## Масштабирование и производительность

### Оптимизации

1. **React.memo** для дорогих компонентов
2. **useMemo** / **useCallback** для стабильных ссылок
3. **FlatList** с правильной оптимизацией (keyExtractor, getItemLayout)
4. **Code splitting** через React.lazy (где возможно)
5. **Image optimization** через expo-image
6. **AsyncStorage** кэширование для редко меняющихся данных

### Метрики

- **Bundle size**: < 10MB (текущий: ~5MB)
- **App startup**: < 3 секунды
- **Screen render**: < 16ms (60 FPS)
- **API response**: < 500ms (под контролем бэкенда)

---

## Безопасность

### Меры безопасности

1. **Аутентификация**: JWT токены с refresh механизмом
2. **Хранилище**: Токены в AsyncStorage (защищённое на уровне ОС)
3. **HTTPS only**: Все API запросы через HTTPS
4. **Валидация**: Строгая валидация всех пользовательских вводов
5. **Secret management**: Sensitive данные в env переменных (не в Git)

### Обработка ошибок

1. **API errors**: Централизованная обработка в API сервисе
2. **Network errors**: Retry механизм, offline detection
3. **User errors**: Понятные сообщения об ошибках
4. **Crash reporting**: Sentry/Bugsnag (будущее)

---

## Следующие шаги

### Краткосрочные (1-2 месяца)

- [ ] Завершить модуль "Сотрудники"
- [ ] Интегрировать с реальным API
- [ ] Добавить unit/integration тесты
- [ ] Настроить E2E тесты (Detox)

### Среднесрочные (3-6 месяцев)

- [ ] Модуль "ПВЗ" (управление пунктами выдачи)
- [ ] Модуль "Чат" (внутренняя коммуникация)
- [ ] Owner Dashboard
- [ ] Push-уведомления

### Долгосрочные (6+ месяцев)

- [ ] Offline-first архитектура
- [ ] GraphQL вместо REST
- [ ] Микрофронтенды (code splitting по модулям)
- [ ] Web версия (React Native Web)

---

## Дополнительные ресурсы

- [README.md](../README.md) - общий обзор проекта
- [CONTRIBUTING.md](../guides/CONTRIBUTING.md) - руководство по разработке
- [TESTING.md](../guides/TESTING.md) - руководство по тестированию
- [design-system/DESIGN_SYSTEM.md](../design-system/DESIGN_SYSTEM.md) - дизайн-система
- [EMPLOYEES_MODULE_ARCHITECTURE_RU.md](../EMPLOYEES_MODULE_ARCHITECTURE_RU.md) - архитектура модуля сотрудников

---

**Последнее обновление**: 3 ноября 2025  
**Автор**: Development Team  
**Статус**: ✅ Актуально
