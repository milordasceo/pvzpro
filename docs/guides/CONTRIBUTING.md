# 🤝 Руководство по внесению изменений

> Contributing Guide для проекта WB ПВЗ

**Версия**: 1.0.0  
**Дата**: 3 ноября 2025

---

## 📋 Содержание

- [Добро пожаловать](#добро-пожаловать)
- [Как внести изменения](#как-внести-изменения)
- [Workflow](#workflow)
- [Стиль коммитов](#стиль-коммитов)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Code Review](#code-review)
- [Использование AI-инструментов](#использование-ai-инструментов)

---

## Добро пожаловать

Спасибо за интерес к проекту WB ПВЗ! Это руководство поможет вам начать работу и внести свой вклад в проект.

### Прежде чем начать

1. Прочитайте [README.md](README.md) - общий обзор проекта
2. Изучите [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) - архитектура проекта
3. Ознакомьтесь с [.cursorrules](.cursorrules) - правила разработки
4. Посмотрите [DECISIONS.md](DECISIONS.md) - архитектурные решения

### Кодекс поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Помогайте новичкам
- Фокусируйтесь на коде, а не на людях

---

## Как внести изменения

### Типы вкладов

**🐛 Исправление багов**
- Нашли баг? Создайте Issue с воспроизведением
- Хотите исправить? Создайте PR с фиксом и тестом

**✨ Новая функциональность**
- Сначала создайте Issue для обсуждения
- Дождитесь одобрения перед началом разработки
- Создайте PR с реализацией, тестами и документацией

**📝 Документация**
- Улучшение существующей документации
- Добавление примеров
- Исправление опечаток

**🎨 UI/UX улучшения**
- Улучшение дизайна
- Accessibility
- Производительность

**♻️ Рефакторинг**
- Улучшение структуры кода
- Оптимизация производительности
- Устранение технического долга

---

## Workflow

### 1. Настройка окружения

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/wbapp.git
cd wbapp

# Установить зависимости
npm install

# Запустить проект
npm run dev
```

### 2. Создание ветки

Используйте следующий формат для веток:

```bash
# Для новой функции
git checkout -b feature/employee-profile-screen

# Для исправления бага
git checkout -b fix/employee-list-empty-state

# Для рефакторинга
git checkout -b refactor/employee-card-component

# Для документации
git checkout -b docs/update-architecture-guide

# Для работы с зависимостями
git checkout -b chore/update-react-navigation
```

**Формат**: `<type>/<short-description>`

**Типы**:
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `refactor/` - рефакторинг без изменения поведения
- `docs/` - изменения в документации
- `test/` - добавление/изменение тестов
- `chore/` - обновление зависимостей, конфигураций
- `perf/` - оптимизация производительности
- `style/` - форматирование кода (не CSS)

### 3. Внесение изменений

#### Правила разработки

1. **Следуйте существующему стилю кода**
   - Используйте ESLint и Prettier
   - Проверьте `.cursorrules` для специфичных правил

2. **Пишите чистый код**
   - Понятные имена переменных и функций
   - Короткие функции (< 50 строк)
   - Один файл = одна ответственность

3. **Комментируйте сложную логику**
   - Объясняйте "почему", а не "что"
   - Используйте JSDoc для публичных API

4. **Типизируйте всё (TypeScript)**
   - Запрещён `any`
   - Все функции должны иметь типы
   - Используйте интерфейсы для объектов

#### Проверка кода

```bash
# Проверка TypeScript
npm run typecheck

# Проверка ESLint
npm run lint

# Проверка форматирования
npm run format

# Запуск тестов (когда будут настроены)
npm test
```

### 4. Создание коммитов

#### Формат коммита

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Примеры**:

```bash
# Новая функция
git commit -m "feat(employees): add employee profile screen"

# Исправление бага
git commit -m "fix(employees): handle empty employee list state"

# Рефакторинг
git commit -m "refactor(components): extract EmployeeCard component"

# Документация
git commit -m "docs(architecture): update module dependencies diagram"

# Обновление зависимостей
git commit -m "chore(deps): update react-navigation to v7.1.19"
```

#### Типы коммитов

- `feat` - новая функциональность
- `fix` - исправление бага
- `refactor` - рефакторинг кода
- `docs` - изменения в документации
- `test` - добавление/изменение тестов
- `chore` - рутинные задачи (зависимости, конфиги)
- `perf` - оптимизация производительности
- `style` - форматирование кода
- `ci` - изменения в CI/CD
- `build` - изменения в сборке

#### Scope (область)

- `employees` - модуль сотрудников
- `auth` - аутентификация
- `dashboard` - dashboard
- `navigation` - навигация
- `components` - UI компоненты
- `store` - state management
- `api` - API сервисы
- `deps` - зависимости

#### Subject (описание)

- Используйте повелительное наклонение: "add", "fix", "update"
- Не заканчивайте точкой
- Максимум 72 символа
- Пишите на английском (для консистентности)

#### Body (тело, опционально)

Добавляйте body для сложных изменений:

```
feat(employees): add employee profile screen

Add comprehensive employee profile screen with following sections:
- Personal information
- Work statistics
- Shift history (last 30 days)
- Contact information

Implements requirements from EMPLOYEES_MODULE_IMPLEMENTATION_PLAN_RU.md
```

#### Footer (футер, опционально)

Используйте для:
- Ссылки на Issues: `Closes #123`
- Breaking changes: `BREAKING CHANGE: description`
- Другие мета-данные

### 5. Создание Pull Request

#### Перед созданием PR

```bash
# Убедитесь что код актуален
git checkout main
git pull origin main
git checkout your-feature-branch
git rebase main

# Проверьте всё локально
npm run typecheck
npm run lint
npm run format
npm test  # когда будут тесты

# Запустите приложение
npm run dev
```

#### Создание PR

1. Перейдите на GitHub
2. Создайте Pull Request из вашей ветки в `main`
3. Заполните шаблон PR (см. ниже)
4. Добавьте соответствующие labels
5. Назначьте reviewers

#### Шаблон Pull Request

```markdown
## Описание изменений
[Что сделано в этом PR]

## Мотивация
[Почему это нужно, какую проблему решает]

## Тип изменений
- [ ] Bugfix (исправление бага)
- [ ] Feature (новая функциональность)
- [ ] Refactoring (рефакторинг)
- [ ] Documentation (документация)
- [ ] Other (другое)

## Как тестировать
1. [Шаг 1]
2. [Шаг 2]
3. [Ожидаемый результат]

## Скриншоты (если применимо)
[Добавьте скриншоты до/после для UI изменений]

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Тесты добавлены/обновлены
- [ ] Документация обновлена
- [ ] ESLint проходит без ошибок
- [ ] TypeScript проходит без ошибок
- [ ] Все тесты зелёные
- [ ] Обратная совместимость сохранена (если применимо)
- [ ] Проверено на Android
- [ ] Проверено на iOS (если доступно)

## Связанные Issues
Closes #[номер issue]

## Breaking Changes
[Если есть breaking changes - опишите здесь]

## Дополнительные заметки
[Любая дополнительная информация для reviewers]
```

---

## Стиль коммитов

### Хорошие примеры ✅

```bash
feat(employees): add search functionality to employee list
fix(dashboard): correct metrics calculation for active shifts
refactor(components): extract common Button component
docs(readme): update installation instructions
test(hooks): add tests for useEmployeeFilter hook
chore(deps): update expo to version 54.0.21
perf(employees): optimize employee list rendering with memo
```

### Плохие примеры ❌

```bash
update files          # Неинформативно
Fixed bug            # Нет scope, нет описания
Add stuff            # Слишком расплывчато
EMPLOYEES SCREEN     # Неправильный формат, caps
Добавил экран        # Не на английском
```

---

## Стандарты кода

### TypeScript

```typescript
// ✅ Хорошо: явные типы, интерфейсы
interface Employee {
  id: string;
  name: string;
  position: string;
  status: 'active' | 'inactive';
}

function getEmployee(id: string): Promise<Employee> {
  return api.get<Employee>(`/employees/${id}`);
}

// ❌ Плохо: any, без типов
function getEmployee(id) {
  return api.get(`/employees/${id}`);
}
```

### React Components

```typescript
// ✅ Хорошо: типизированные props, FC
import React from 'react';
import { View, Text } from 'react-native';
import { Button } from 'react-native-paper';

interface EmployeeCardProps {
  employee: Employee;
  onPress?: (employee: Employee) => void;
}

export const EmployeeCard: React.FC<EmployeeCardProps> = ({ 
  employee, 
  onPress 
}) => {
  return (
    <View>
      <Text>{employee.name}</Text>
      {onPress && (
        <Button onPress={() => onPress(employee)}>View</Button>
      )}
    </View>
  );
};

// ❌ Плохо: без типов, неструктурированный код
export const EmployeeCard = (props) => {
  return (
    <View>
      <Text>{props.employee.name}</Text>
    </View>
  );
};
```

### Naming Conventions

```typescript
// Файлы
EmployeeCard.tsx          // ✅ PascalCase для компонентов
useEmployees.ts           // ✅ camelCase для хуков
formatDate.ts             // ✅ camelCase для утилит
employeeStore.ts          // ✅ camelCase для stores

// Переменные и функции
const userName = 'John';  // ✅ camelCase
const MAX_RETRIES = 3;    // ✅ UPPER_SNAKE_CASE для констант

// Типы и интерфейсы
interface IUser {}        // ✅ PascalCase с I для интерфейсов
type UserType = {};       // ✅ PascalCase для типов

// Компоненты
const EmployeeCard = () => {}  // ✅ PascalCase
```

### Imports организация

```typescript
// 1. React импорты
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';

// 2. Сторонние библиотеки
import { Button, Card } from 'react-native-paper';

// 3. Навигация
import { useNavigation } from '@react-navigation/native';

// 4. Компоненты
import { EmployeeCard } from '../components/EmployeeCard';

// 5. Хуки
import { useEmployees } from '../hooks/useEmployees';

// 6. Store
import { useAuthStore } from '../store/authStore';

// 7. Утилиты
import { formatDate } from '../utils/formatDate';

// 8. Типы
import type { Employee } from '../types/models';

// 9. Стили (если в отдельном файле)
import styles from './styles';
```

---

## Тестирование

> **Примечание**: Тесты пока в процессе настройки. См. [TESTING.md](TESTING.md)

### Требования к тестам

**При исправлении багов**:
- ОБЯЗАТЕЛЬНО добавить регрессионный тест
- Тест должен падать на старом коде
- Тест должен проходить после фикса

**При добавлении новой функциональности**:
- Целевое покрытие: ≥80%
- Unit тесты для утилит и хуков
- Component тесты для UI компонентов
- Integration тесты для критичных потоков

**При рефакторинге**:
- Все существующие тесты должны проходить
- Покрытие не должно уменьшаться

### Пример теста

```typescript
// src/utils/__tests__/formatDate.test.ts
import { formatDate } from '../formatDate';

describe('formatDate', () => {
  it('should format date to DD.MM.YYYY', () => {
    const date = new Date('2025-11-03');
    expect(formatDate(date)).toBe('03.11.2025');
  });

  it('should handle invalid date', () => {
    expect(formatDate(null)).toBe('—');
  });
});
```

---

## Code Review

### Для авторов PR

**Перед запросом review**:
- [ ] Код соответствует стилю проекта
- [ ] Все checks проходят (CI)
- [ ] PR описание заполнено
- [ ] Добавлены скриншоты (для UI)
- [ ] Обновлена документация (если нужно)

**Во время review**:
- Отвечайте на комментарии быстро
- Будьте открыты к критике
- Объясняйте сложные решения
- Вносите запрошенные изменения

### Для reviewers

**Что проверять**:
1. **Функциональность**: PR решает заявленную задачу?
2. **Код**: читаемый, поддерживаемый, следует стилю?
3. **Архитектура**: соответствует паттернам проекта?
4. **Производительность**: нет очевидных проблем?
5. **Безопасность**: нет уязвимостей, секретов?
6. **Тесты**: достаточное покрытие?
7. **Документация**: обновлена если нужно?

**Как давать feedback**:
- Будьте конструктивны
- Объясняйте "почему"
- Предлагайте альтернативы
- Хвалите хорошие решения

**Типы комментариев**:
```
🔴 CRITICAL: Это нужно исправить обязательно
🟠 MAJOR: Рекомендуется исправить
🟡 MINOR: Можно улучшить
💡 SUGGESTION: Опциональное предложение
✅ PRAISE: Отличное решение!
❓ QUESTION: Вопрос для понимания
```

**Примеры комментариев**:

```markdown
🔴 CRITICAL: Здесь возможна утечка памяти из-за отсутствия cleanup в useEffect
Предложение: добавьте return () => { cleanup() }

🟠 MAJOR: Эта функция может быть оптимизирована с помощью useMemo
Текущая: пересчёт на каждом рендере
Предложение: const filtered = useMemo(() => filterEmployees(data), [data])

💡 SUGGESTION: Можно использовать optional chaining для упрощения
employee.address && employee.address.city
→ employee.address?.city

✅ PRAISE: Отличная обработка edge cases!
```

---

## Использование AI-инструментов

Мы поддерживаем использование AI инструментов (Cursor, GitHub Copilot, ChatGPT) для ускорения разработки.

### Правила использования AI

1. **Читайте .cursorrules**
   - Все правила проекта задокументированы в [.cursorrules](.cursorrules)
   - AI должен следовать этим правилам

2. **Используйте промпт-шаблоны**
   - См. [docs/AI_PROMPTS.md](docs/AI_PROMPTS.md)
   - Готовые шаблоны для типовых задач

3. **Проверяйте сгенерированный код**
   - AI может ошибаться
   - Всегда review код перед коммитом
   - Запускайте линтеры и тесты

4. **Не коммитьте AI артефакты**
   - Не добавляйте AI комментарии в продакшн код
   - Убирайте debug console.log

### Примеры использования AI

**✅ Хорошо**:
```typescript
// Запрос к AI:
// "Создай Employee profile screen следуя docs/EMPLOYEES_MODULE_IMPLEMENTATION_PLAN_RU.md
// и используй React Native Paper компоненты"

// AI создаёт код
// Вы review и тестируете
// Коммитите только проверенный код
```

**❌ Плохо**:
```typescript
// Запрос: "Создай экран"
// AI создаёт код с другими UI библиотеками
// Вы коммитите без проверки
// Code review находит несоответствия стилю
```

### AI для Code Review

AI можно использовать для предварительного review:

```
Задача: Проанализировать код и предложить улучшения
Контекст: @file src/screens/EmployeeProfileScreen.tsx
Фокус: производительность, безопасность, читаемость

Вывод: bullet list с приоритетами
```

---

## Полезные команды

```bash
# Разработка
npm run dev              # Запуск с очисткой кэша
npm start                # Обычный запуск
npm run start:tunnel     # Через tunnel

# Проверка кода
npm run typecheck        # TypeScript
npm run lint             # ESLint
npm run format           # Prettier check

# Тесты (когда будут настроены)
npm test                 # Запуск тестов
npm run test:watch       # Watch mode
npm run test:coverage    # Coverage отчёт

# Сборка
npm run android          # Android build
npm run ios              # iOS build (требует Mac)
```

---

## Получение помощи

### Документация

- [README.md](README.md) - общий обзор
- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) - архитектура
- [TESTING.md](TESTING.md) - тестирование
- [DECISIONS.md](DECISIONS.md) - архитектурные решения
- [docs/](docs/) - дополнительная документация

### Где задать вопрос?

1. **GitHub Issues** - для багов и feature requests
2. **GitHub Discussions** - для вопросов и обсуждений
3. **Pull Request comments** - для вопросов по коду
4. **Team Slack/Discord** - для быстрых вопросов

### Создание Issue

**Для багов**:
```markdown
**Описание**: [что не работает]
**Шаги воспроизведения**: 
1. ...
2. ...
**Ожидаемое**: [что должно быть]
**Наблюдаемое**: [что происходит]
**Платформа**: Android 14 / iOS 17
**Версия приложения**: 2.1.0
**Скриншоты**: [если есть]
```

**Для feature requests**:
```markdown
**Проблема**: [какую проблему решает]
**Предлагаемое решение**: [как можно решить]
**Альтернативы**: [другие варианты]
**Дополнительный контекст**: [любая дополнительная информация]
```

---

## Чеклист контрибьютора

Перед созданием PR убедитесь:

### Код
- [ ] Следует стилю проекта (.cursorrules)
- [ ] TypeScript типы добавлены
- [ ] ESLint проходит без ошибок
- [ ] Prettier применён
- [ ] Нет console.log в продакшн коде
- [ ] Нет закомментированного кода

### Функциональность
- [ ] Работает на Android
- [ ] Работает на iOS (если доступно)
- [ ] Обрабатывает edge cases
- [ ] Обрабатывает ошибки
- [ ] Loading states добавлены

### Тесты
- [ ] Unit тесты добавлены (если применимо)
- [ ] Component тесты добавлены (для UI)
- [ ] Все тесты проходят
- [ ] Покрытие ≥80% (для новой функциональности)

### Документация
- [ ] README обновлён (если нужно)
- [ ] Inline комментарии добавлены (для сложной логики)
- [ ] ADR создан (для архитектурных решений)
- [ ] CHANGELOG обновлён (для major изменений)

### Git
- [ ] Commit messages следуют Conventional Commits
- [ ] Ветка названа правильно (type/description)
- [ ] PR описание заполнено
- [ ] Squash commits если нужно

---

## Спасибо!

Спасибо за ваш вклад в проект! Каждый PR, Issue, и комментарий помогает сделать проект лучше. 🎉

---

**Вопросы?** Создайте Issue или напишите в Discussions.

**Последнее обновление**: 3 ноября 2025
