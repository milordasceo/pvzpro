# Дизайн-система WB ПВЗ

> **Обновлено:** 2025-11-01  
> **Статус:** Актуально (Оптимизирована: 29 → 22 цвета, новая типографика)

---

## 📚 Навигация по документации

Эта дизайн-система состоит из нескольких специализированных гайдов:

### Основные гайды
- **[COLOR_USAGE_GUIDE.md](COLOR_USAGE_GUIDE.md)** - Полное руководство по цветам (22 цвета)
- **[TYPOGRAPHY_GUIDE.md](TYPOGRAPHY_GUIDE.md)** - Типографическая система и компоненты
- **[LAYOUT_GUIDE.md](LAYOUT_GUIDE.md)** - Принципы размещения элементов
- **[UI_GUIDELINES.md](UI_GUIDELINES.md)** - Правила использования UI компонентов
- **[UI_COMPONENTS.md](UI_COMPONENTS.md)** - Каталог всех компонентов

### Быстрый доступ
- **[DESIGN_QUICK_REF.md](DESIGN_QUICK_REF.md)** - Шпаргалка (1 страница)

---

## Краткая сводка

### Философия дизайна
1. **Минимализм** - используем только необходимое
2. **Консистентность** - единый стиль во всём приложении
3. **Производительность** - оптимизированные компоненты
4. **Масштабируемость** - легко расширять и поддерживать

### Ключевые принципы
- **UI-first подход** - сначала верстка, затем логика
- **React Native Paper** - базовая библиотека UI
- **Design Tokens** - централизованная система цветов и spacing
- **Типографические компоненты** - вместо инлайн-стилей
- **Layout утилиты** - для частых паттернов

---

## 🎨 Цвета

**Всего: 22 цвета** (оптимизировано с 29)

### Основные категории
- **Primary** (3) - основной бренд #4F46E5
- **Gray** (6) - нейтральная шкала
- **Success** (5) - положительные статусы #10B981
- **Warning** (3) - предупреждения #F59E0B
- **Error** (4) - ошибки #DC2626
- **Info** (3) - информация #3B82F6

### Использование
```tsx
import { tokens } from '../ui';

tokens.colors.primary.main     // Основные кнопки, акценты
tokens.colors.text.primary     // Заголовки, основной текст
tokens.colors.text.secondary   // Вторичный текст
tokens.colors.screenBackground // Фон экранов
tokens.colors.surface          // Фон карточек
```

**Подробно:** [COLOR_USAGE_GUIDE.md](COLOR_USAGE_GUIDE.md)

---

## ✏️ Типографика

### Компоненты

```tsx
import { Heading, Title, Body, Label, Caption } from '../ui';

<Heading level={1}>Главный заголовок</Heading>      // ~32px
<Heading level={2}>Подзаголовок</Heading>           // ~28px
<Title size="medium">Заголовок карточки</Title>     // ~16px
<Body>Основной текст</Body>                         // ~14px
<Label>Метка поля</Label>                           // ~12px
<Caption>Вторичная информация</Caption>             // ~12px secondary
```

### Иерархия
1. **Heading 1-3** - главные заголовки (32px/28px/24px)
2. **Title** - заголовки секций/карточек (22px/16px/14px)
3. **Body** - основной текст (16px/14px/12px)
4. **Label** - метки (14px/12px/11px)
5. **Caption** - вторичная информация (12px)

**Подробно:** [TYPOGRAPHY_GUIDE.md](TYPOGRAPHY_GUIDE.md)

---

## 📐 Spacing и Layout

### Базовая сетка: 4px

Все отступы кратны 4px.

### Токены spacing

```tsx
tokens.spacing.xs             // 4px
tokens.spacing.sm             // 8px
tokens.spacing.md             // 12px
tokens.spacing.lg             // 16px
tokens.spacing.xl             // 24px

tokens.spacing.screenPadding  // 16px - padding экрана
tokens.spacing.cardGap        // 12px - между карточками
tokens.spacing.sectionGap     // 16px - между секциями
```

### Layout утилиты

```tsx
import { screenContainer, horizontalStack, verticalStack } from '../ui';

<View style={screenContainer}>           // flex: 1 + фон экрана
<View style={horizontalStack()}>         // flexDirection: row + gap
<View style={verticalStack(12)}>         // gap: 12
```

**Подробно:** [LAYOUT_GUIDE.md](LAYOUT_GUIDE.md)

---

## 🧩 Компоненты

### Основные компоненты

```tsx
import { 
  Button, IconButton,      // Кнопки
  Card, ScrollView,        // Контейнеры
  SearchInput, SelectModal, // Inputs
  Dialog,                  // Overlays
  TabBar, Tab,             // Навигация
  StatusBadge,             // Feedback
  EmptyState, ErrorState,  // States
  Heading, Title, Body,    // Typography
} from '../ui';
```

### Правила использования
- ✅ Использовать компоненты из `../ui`
- ✅ Paper компоненты (Text, Badge, Chip, Avatar) напрямую
- ❌ НЕ создавать кастомные аналоги существующих компонентов
- ❌ НЕ использовать Searchbar из Paper (медленный)

**Подробно:** [UI_COMPONENTS.md](UI_COMPONENTS.md), [UI_GUIDELINES.md](UI_GUIDELINES.md)

## Токены (`src/ui/themeTokens.ts`)
- `radius = 8` — базовое скругление
- `controlHeight = 48` — поля ввода, поиски, иконки-кнопки
- `buttonHeight = 44` — основные кнопки
- `gap = 12` — стандартный вертикальный отступ

## Цветовая палитра (`src/theme.ts`)
- `primary: #4F46E5`
- `secondary: #7C3AED`
- `placeholderColor: #6B7280`
- `outline: #E5E7EB`

## Унифицированные компоненты (`src/components`)
- `StyledButton` — Paper Button с токенами
- `StyledCard` — Paper Card с отступами и заголовками
- `StyledDialog` — диалог с округлыми углами, слотами действий
  - **Важно:** действия (actions) должны быть обернуты в `<View>` для корректной работы
- `StyledScrollView` — ScrollView с паддингами/gap без правого индикатора
- `SquareIconButton`, `MetaRow` и др.

## Правила использования
- Кнопки/контролы — использовать `StyledButton` (primary 44 px высоты)
- Вторичные кнопки — `mode="text" | "outlined"` в `StyledButton`
- Поля поиска/ввода — высота 48 px, placeholder серый `#6B7280`
- Списки — `showsVerticalScrollIndicator={false}`
- Диалоги — через `StyledDialog`, подтверждение критических действий там
  - Действия в `StyledDialog` должны быть обёрнуты в `<View style={{ flexDirection: 'row', gap: 8 }}>`
- В Expo Go push-уведомления не работают; UI не должен зависеть от них

## Chat / Финансы / График
- Пузырь чата: радиус 8, `#F3F4F6` слева, `#E0E7FF` справа
- Финансовые карточки — `StyledCard`, `MetaRow`, `StyledButton` для действий
- График — кнопки навигации текстовые, выбор дат: зелёный фон `rgba(22,163,74,0.18)`

## Типографика
- Paper `Text` (MD3): `titleMedium`, `titleLarge`, `bodyMedium`
- План — вынести типографические токены в `src/ui/themeTokens.ts`

## Иконки
- `@expo/vector-icons` (MaterialCommunityIcons)
- Иконки в кнопках/MetaRow — через Paper/IconButton или `SquareIconButton`

## Поведение/UX

### Сканер QR
- Оверлей, подсказка, кнопка фонарика
- QR коды генерируются с HMAC и TTL 5 минут

### Перерывы
- Счётчик/лимит (3×10 мин)
- Единый стиль Snackbar для уведомлений
- Автозавершение перерыва по истечении времени

### Завершение смены (обновлено 2025-10-01)
- **Без жесткой блокировки** по времени или незавершенным задачам
- **Умные предупреждения** в диалоге подтверждения:
  - 🟡 Желтый блок: предупреждение о незавершенных задачах
  - 🔵 Синий блок: информация о раннем завершении
- Цветовые блоки для визуального разделения типов информации

### Чат
- Оверлейная панель, смайлы, вложения
- Fullscreen режим: Appbar скрывается на экране чата

## Планы
- Добавить унифицированный `StyledTextInput`, `StyledSnackbar`
- Вынести типографику в токены (размеры/веса)
- Подготовить каталог компонентов (Storybook/Docs) при необходимости

---

---

**📚 Для подробной информации см. актуальные документы:**
- [`UI_GUIDELINES.md`](UI_GUIDELINES.md) - правила использования
- [`UI_TOKENS.md`](UI_TOKENS.md) - design tokens
- [`UI_COMPONENTS.md`](UI_COMPONENTS.md) - каталог компонентов
