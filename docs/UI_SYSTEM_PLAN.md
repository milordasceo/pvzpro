# 🎨 План создания единой системы UI

**Дата создания:** 19 октября 2025  
**Цель:** Создать унифицированную, производительную систему UI компонентов для всего приложения

---

## 🎯 Проблема

Сейчас в проекте:
- ❌ Разные реализации одних и тех же элементов
- ❌ Иногда создаются кастомные компоненты вместо использования существующих
- ❌ Нет единого стиля для всех экранов
- ❌ Медленные компоненты (например, Searchbar из react-native-paper)
- ❌ Дублирование кода

---

## ✅ Цель

Создать единую библиотеку компонентов:
- ✅ Единообразный дизайн
- ✅ Высокая производительность
- ✅ Легкость использования
- ✅ Полная документация
- ✅ TypeScript типизация
- ✅ Примеры использования

---

## 📦 Что уже есть

### Базовая библиотека: React Native Paper
Мы используем **React Native Paper** как основу:
- `Text`, `Button`, `Card`, `Dialog`, `Portal`, `Modal` и др.
- Компоненты Paper используются НАПРЯМУЮ когда они подходят
- Создаём обёртки (Styled*) только когда нужна кастомизация

### Кастомные обёртки (src/components/):
1. ✅ **StyledCard** - обёртка над Paper Card с доп. стилями
2. ✅ **StyledButton** - обёртка над Paper Button с вариантами
3. ✅ **StyledScrollView** - скролл с кастомными возможностями
4. ✅ **StyledDialog** - обёртка над Paper Dialog
5. ✅ **SquareIconButton** - квадратные кнопки с иконками
6. ✅ **MetaRow** - строка с метаданными
7. ✅ **SelectModal** - модал выбора
8. ✅ **AnimatedTabBar** - анимированные табы (кастомный)

### Что нужно добавить:

**Важно:** Используем Paper компоненты когда они подходят!
- Paper `Badge` - используем напрямую ✅
- Paper `Chip` - используем напрямую ✅
- Paper `Avatar` - используем напрямую ✅
- Paper `ActivityIndicator` - используем напрямую ✅

**Создаём свои только если нужна кастомизация:**
- [ ] **SearchInput** - обёртка над TextInput с иконками
- [ ] **FilterBar** - обёртка над AnimatedTabBar для фильтров
- [ ] **EmptyState** - композитный компонент (иконка + текст + кнопка)
- [ ] **LoadingState** - обёртка над ActivityIndicator
- [ ] **ErrorState** - композитный компонент для ошибок
- [ ] **StatusBadge** - обёртка над Paper Badge с предустановками
- [ ] **ListItem** - обёртка над Paper List.Item
- [ ] **SectionHeader** - обёртка над Paper Subheading
- [ ] **ActionSheet** - кастомный bottom sheet
- [ ] **Toast** - кастомные уведомления (Paper Snackbar базово)
- [ ] **Skeleton** - кастомный skeleton loading

---

## 🏗️ Структура новой системы

```
src/
├── ui/                           # Новая папка для UI системы
│   ├── index.ts                  # Экспорт всех компонентов
│   ├── theme/
│   │   ├── colors.ts             # Палитра цветов
│   │   ├── spacing.ts            # Отступы и размеры
│   │   ├── typography.ts         # Типографика
│   │   └── tokens.ts             # Design tokens
│   ├── components/
│   │   ├── feedback/             # Компоненты обратной связи
│   │   │   ├── Badge.tsx
│   │   │   ├── StatusBadge.tsx
│   │   │   ├── Toast.tsx
│   │   │   └── Skeleton.tsx
│   │   ├── inputs/               # Компоненты ввода
│   │   │   ├── SearchInput.tsx
│   │   │   ├── FilterBar.tsx
│   │   │   └── Chip.tsx
│   │   ├── layout/               # Компоненты макета
│   │   │   ├── Screen.tsx
│   │   │   ├── Section.tsx
│   │   │   ├── ListItem.tsx
│   │   │   └── SectionHeader.tsx
│   │   ├── states/               # Состояния
│   │   │   ├── EmptyState.tsx
│   │   │   ├── LoadingState.tsx
│   │   │   └── ErrorState.tsx
│   │   └── overlays/             # Оверлеи
│   │       ├── ActionSheet.tsx
│   │       └── Modal.tsx
│   └── hooks/                    # Хуки для UI
│       ├── useTheme.ts
│       ├── useToast.ts
│       └── useActionSheet.ts
```

---

## 📋 План разработки

### Этап 1: Подготовка (1 сессия)
- [ ] Создать структуру папок
- [ ] Перенести theme tokens в отдельные файлы
- [ ] Создать единую палитру цветов
- [ ] Создать систему spacing
- [ ] Создать типографику

### Этап 2: Базовые компоненты (2-3 сессии)
- [ ] SearchInput (замена Searchbar)
- [ ] FilterBar (использует AnimatedTabBar)
- [ ] Badge
- [ ] StatusBadge
- [ ] Avatar

### Этап 3: Компоненты состояний (1 сессия)
- [ ] EmptyState
- [ ] LoadingState
- [ ] ErrorState
- [ ] Skeleton

### Этап 4: Компоненты макета (1-2 сессии)
- [ ] Screen (обёртка для экранов)
- [ ] Section (секции с заголовками)
- [ ] ListItem (универсальный элемент списка)
- [ ] SectionHeader

### Этап 5: Дополнительные компоненты (2 сессии)
- [ ] ActionSheet
- [ ] Toast
- [ ] Chip
- [ ] Modal (улучшенная версия)

### Этап 6: Документация (1 сессия)
- [ ] README для каждого компонента
- [ ] Storybook или примеры использования
- [ ] Обновить UI_GUIDELINES.md
- [ ] Создать UI_COMPONENTS.md (каталог)

### Этап 7: Миграция (2-3 сессии)
- [ ] Обновить существующие экраны
- [ ] Удалить дублирующийся код
- [ ] Рефакторинг Dashboard
- [ ] Рефакторинг Schedule
- [ ] Создать модуль Сотрудники заново (правильно)

---

## 🎨 Design System

### Цвета

#### Primary (Фиолетовый):
```tsx
primary: {
  50: '#F5F3FF',
  100: '#EDE9FE',
  200: '#DDD6FE',
  300: '#C4B5FD',
  400: '#A78BFA',
  500: '#8B5CF6',  // Main
  600: '#7C3AED',
  700: '#6D28D9',
  800: '#5B21B6',
  900: '#4C1D95',
}
```

#### Gray (Серый):
```tsx
gray: {
  50: '#F9FAFB',
  100: '#F3F4F6',
  200: '#E5E7EB',
  300: '#D1D5DB',
  400: '#9CA3AF',
  500: '#6B7280',  // Main
  600: '#4B5563',
  700: '#374151',
  800: '#1F2937',
  900: '#111827',
}
```

#### Status:
```tsx
success: '#10B981',  // Зелёный
warning: '#F59E0B',  // Жёлтый
error: '#DC2626',    // Красный
info: '#0891B2',     // Синий
```

### Spacing

```tsx
spacing: {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
}
```

### Border Radius

```tsx
radius: {
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
}
```

### Typography

```tsx
fontSize: {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 18,
  xl: 20,
  xxl: 24,
  xxxl: 32,
}

fontWeight: {
  regular: '400',
  medium: '500',
  semibold: '600',
  bold: '700',
}
```

---

## 🚀 Примеры использования

### SearchInput
```tsx
import { SearchInput } from '../ui/components/inputs/SearchInput';

<SearchInput
  value={query}
  onChangeText={setQuery}
  placeholder="Поиск..."
/>
```

### EmptyState
```tsx
import { EmptyState } from '../ui/components/states/EmptyState';

<EmptyState
  icon="account-off"
  title="Нет сотрудников"
  description="Добавьте первого сотрудника"
  action={{
    label: 'Добавить',
    onPress: handleAdd,
  }}
/>
```

### StatusBadge
```tsx
import { StatusBadge } from '../ui/components/feedback/StatusBadge';

<StatusBadge status="success">На смене</StatusBadge>
<StatusBadge status="warning">Опоздание</StatusBadge>
<StatusBadge status="error">Неактивен</StatusBadge>
```

---

## ✅ Преимущества

1. **Единообразие** - все экраны выглядят одинаково
2. **Производительность** - оптимизированные компоненты
3. **Скорость разработки** - не нужно создавать UI с нуля
4. **Легкость поддержки** - изменения в одном месте
5. **TypeScript** - полная типизация
6. **Документация** - примеры для каждого компонента

---

## 📚 Документация

После создания системы будет:
- `docs/UI_SYSTEM.md` - обзор системы
- `docs/UI_COMPONENTS.md` - каталог всех компонентов
- `docs/UI_GUIDELINES.md` - правила использования (уже есть)
- README в каждом компоненте

---

## 🎯 Критерии успеха

- [ ] Все базовые UI элементы унифицированы
- [ ] Нет дублирования кода
- [ ] Все компоненты мемоизированы
- [ ] Документация для каждого компонента
- [ ] Примеры использования
- [ ] TypeScript типизация 100%
- [ ] Все существующие экраны обновлены

---

**Готов к началу работы!** 🚀

**Следующая сессия:** Этап 1 - Подготовка (создание структуры и theme tokens)

