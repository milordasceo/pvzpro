# 📋 Правила проекта WB App (v2)

Этот документ является **единым источником правды** для разработки. Все изменения в проекте должны соответствовать этим правилам.

## 1. 🛠 Технологический стек (Performance First)

Мы используем современный стек для достижения максимальной производительности (60 FPS) и качественного UI.

*   **Framework:** React Native 0.81+ (Expo SDK 54).
*   **UI Library:** HeroUI Native (Beta).
*   **Styling Engine:** Uniwind (Tailwind CSS v4 features) — для производительности (компиляция стилей).
*   **Animations:** React Native Reanimated v4.
*   **Icons:** Lucide React Native (или из HeroUI).
*   **State Management:** Zustand (легковесный).
*   **Architecture:** Feature-Sliced Design (FSD) — упрощенная версия.

## 2. ⚡ Принципы разработки

### UI First (Первоочередность интерфейса)
1.  Сначала верстаем идеальный UI/UX по дизайну.
2.  Используем моковые данные (mock data) для проверки верстки.
3.  Только после утверждения UI подключаем логику и API.
4.  **Стиль:** Используем токены HeroUI и классы Tailwind. Избегаем `StyleSheet.create` без необходимости.

### Performance (Производительность во главе угла)
1.  Избегаем лишних ре-рендеров.
2.  Используем `FlashList` вместо `FlatList` для списков.
3.  Оптимизируем изображения (WebP, кэширование).
4.  Избегаем тяжелых JS-вычислений в UI-потоке (используем Worklets).

## 3. 🤖 Взаимодействие с AI

*   **Роль:** Ассистент-реализатор. Предлагает решения, пишет код, ждет подтверждения.
*   **Язык:** Русский. Объяснения простые, пошаговые, для начинающих.
*   **Формат:** "Сделал X -> Проверь -> Делаем Y".
*   **Инициатива:** Если задача понятна — выполняем. Если есть сомнения — предлагаем варианты, но не блокируем работу.

## 4. 📝 Документация и версионирование

*   **Актуальность:** Обновляем документацию *после* успешного тестирования задачи пользователем.
*   **Даты:** Всегда проверяем дату через `Get-Date` перед записью.
    *   Формат: `dd MMMM yyyy - HH:mm` (например, `18 декабря 2025 - 14:30`).
*   **Проверка версий:** Перед внедрением библиотеки проверяем её совместимость с Expo SDK 54 и HeroUI Native.

## 5. 📂 Структура проекта

```
/
├── .github/            # CI/CD
├── assets/             # Статика (картинки, шрифты)
├── docs/               # Документация (RULES.md, TECH_STACK.md)
├── src/
│   ├── app/            # Expo Router (если используется) или App.tsx провайдеры
│   ├── features/       # Бизнес-фичи (auth, profile, orders)
│   ├── shared/         # Общие компоненты (ui-kit), утилиты, api
│   │   ├── ui/         # HeroUI кастомизация
│   │   ├── lib/        # Хелперы (logger, utils)
│   │   └── theme/      # Глобальная тема (цвета, layout)
│   └── widgets/        # Сложные самостоятельные блоки
├── global.css          # Глобальные стили (Uniwind imports)
├── tailwind.config.js  # Конфиг Tailwind
└── babel.config.js     # Конфиг Babel
```

## 6. 🚀 Алгоритм старта задачи
1.  Получить задачу.
2.  Проверить текущее состояние (`git status`, `Get-Date`).
3.  Реализовать UI (если применимо).
4.  Проверить на эмуляторе/устройстве.
5.  Запросить подтверждение.
6.  Обновить `docs/` и закоммитить.

## 7. 🎨 Дизайн-система и UI-Консистентность (Строго)

**Цель:** Единый визуальный стиль для всех ролей (Сотрудник, Менеджер, Владелец).

### Правила:
1.  **NO HARDCODE (Запрет хардкода):**
    *   ❌ Запрещено использовать хардкодные цвета (например, `#006FEE`, `rgb(0,0,0)`).
    *   ❌ Запрещено использовать хардкодные размеры (например, `height: 60`, `padding: 20`).
    *   ✅ **Всегда** использовать `src/shared/theme` (colors, layout) или классы Tailwind (`bg-primary`, `p-4`).
    *   ✅ Для размеров использовать `theme.layout.*` (header height, icon sizes).

2.  **Единый источник компонентов:** Используем ТОЛЬКО компоненты из `heroui-native`.
    *   ✅ `Button`, `TextField`, `Card`, `Text` (из RN или HeroUI Typography).
    *   ❌ Не создавать свои велосипеды для кнопок или инпутов, если они есть в библиотеке.

3.  **Визуальное единообразие:**
    *   Кнопки, шрифты, отступы и цвета должны быть одинаковыми на всех экранах.
    *   Не допускается, чтобы у Сотрудника кнопка была квадратной, а у Менеджера — круглой (если это не семантическое отличие).

4.  **Типографика:**
    *   Следовать иерархии текстов HeroUI и Tailwind (`text-xl`, `font-bold`).

## 8. 🕵️‍♂️ Логирование и Отладка (Обязательно)

Чтобы быстро находить причины ошибок, все ключевые действия пользователя должны быть видны в консоли.

### Правила:
1.  **Логирование навигации:**
    *   Все переходы между экранами должны логироваться в `App.tsx` через `onStateChange`.
    *   Формат: `[ACTION] Navigation: Navigate { to: "ScreenName" }`.

2.  **Логирование действий (Action Logging):**
    *   Все кликабельные элементы (`Button`, `Pressable`, `TouchableOpacity`) должны логировать нажатие.
    *   Использовать обертку `src/shared/ui/Button.tsx` или вызывать `logger.action()` вручную.
    *   Формат: `[ACTION] Button: Pressed { label: "Button Name" }`.

3.  **Утилита:**
    *   Использовать `src/shared/lib/logger.ts` для единообразия логов.
