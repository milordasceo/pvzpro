# 🔧 Исправление ошибки "Text strings must be rendered within a <Text> component"

**Дата:** 2025-10-20
**Статус:** ✅ В процессе

## Проблема

Приложение выдавало ошибку при загрузке:
```
ERROR  Text strings must be rendered within a <Text> component.
```

Стек трейс указывал только на внутренние файлы React Fabric, без указания конкретного файла проекта.

## Причина

В React Native компоненты не могут возвращать `null` напрямую - это вызывает ошибку. Нужно возвращать валидный JSX, например пустой Fragment `<></>`.

## Исправления

### 1. ✅ `src/navigation/AppNavigator.tsx`
**Было:**
```tsx
const LoadingScreen: React.FC = () => {
  return null;
};
```

**Стало:**
```tsx
const LoadingScreen: React.FC = () => {
  return <></>;
};
```

### 2. ✅ `src/navigation/MainNavigator.tsx`
**Было:**
```tsx
if (!user) {
  return null;
}
```

**Стало:**
```tsx
if (!user) {
  return <></>;
}
```

### 3. ✅ `src/ui/components/dev/UICatalogFAB.tsx`
**Было:**
```tsx
if (!visible) {
  return null;
}
```

**Стало:**
```tsx
if (!visible) {
  return <></>;
}
```

### 4. ✅ `src/employee/ScheduleScreen.tsx`
**Было:**
```tsx
return null;
})()}
```

**Стало:**
```tsx
return <></>;
})()}
```

## Дополнительные инструменты

### ErrorBoundary
Создан компонент `src/components/ErrorBoundary.tsx` для отлова ошибок рендеринга с детальным Component Stack.

### Скрипты для отладки
1. **`VIEW_LOGS.bat`** - просмотр логов Android с полными стек-трейсами
2. **`START_WITH_LOGS.bat`** - запуск с автоматическим открытием окна логов
3. **`START_DEBUG.bat`** - запуск Metro с детальным логированием

## Тестирование

После исправлений:
```bash
# TypeScript проверка
npm run typecheck  # ✅ Passed

# Запуск с очисткой кэша
npx expo start --localhost --clear
```

## Примечания

### Правильные паттерны для условного рендеринга:

✅ **Правильно:**
```tsx
// Пустой Fragment
if (!visible) return <></>;

// Условный оператор с Fragment
{visible ? <Component /> : <></>}

// Логическое И с проверкой
{items.length > 0 && <List />}
```

❌ **Неправильно:**
```tsx
// null в React Native
if (!visible) return null;

// undefined
{visible ? <Component /> : undefined}

// Числа могут рендериться (0 отображается!)
{count && <Text>{count}</Text>}  // Если count=0, отобразится "0"
```

### Безопасные return null

`return null` безопасен только в:
- Утилитах и сервисах (не компонентах)
- Функциях обработки данных
- Callback функциях

## Статус

- ✅ TypeScript проверка пройдена
- 🔄 Тестирование на устройстве...

## Следующие шаги

Если ошибка всё ещё появляется:
1. Проверить условные выражения с `&&` (могут рендерить `0`)
2. Проверить третичные операторы возвращающие `undefined`
3. Использовать `START_DEBUG.bat` для детального логирования

