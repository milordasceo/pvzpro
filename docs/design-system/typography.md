# ✏️ Гайд по типографике

> **Обновлено:** 2025-11-01  
> **Новая система:** Типографические компоненты + Paper variants

## 📋 Содержание

1. [Обзор типографической системы](#обзор-типографической-системы)
2. [Типографические компоненты](#типографические-компоненты)
3. [Иерархия текста](#иерархия-текста)
4. [Когда использовать какой компонент](#когда-использовать-какой-компонент)
5. [Цвета текста](#цвета-текста)
6. [Примеры использования](#примеры-использования)
7. [Migration Guide](#migration-guide)

---

## Обзор типографической системы

Типографика в приложении строится на **React Native Paper** с дополнительными компонентами-обёртками для частых паттернов.

**Два подхода:**

1. **Прямое использование Paper Text** (для простых случаев)
2. **Типографические компоненты** (для частых паттернов)

### Новые компоненты

```tsx
import { Heading, Title, Body, Label, Caption } from '../ui';
```

| Компонент | Назначение                | Базируется на                    |
| --------- | ------------------------- | -------------------------------- |
| `Heading` | H1, H2, H3 заголовки      | Paper headlineLarge/Medium/Small |
| `Title`   | Заголовки секций/карточек | Paper titleLarge/Medium/Small    |
| `Body`    | Основной текст            | Paper bodyLarge/Medium/Small     |
| `Label`   | Метки, подписи            | Paper labelLarge/Medium/Small    |
| `Caption` | Мелкий вторичный текст    | Paper bodySmall + цвет           |

---

## Типографические компоненты

### Heading - Основные заголовки

```tsx
import { Heading } from '../ui';

// H1 (headlineLarge, ~32px)
<Heading level={1}>Главный заголовок</Heading>

// H2 (headlineMedium, ~28px)
<Heading level={2}>Подзаголовок</Heading>

// H3 (headlineSmall, ~24px)
<Heading level={3}>Секция</Heading>

// С кастомным стилем
<Heading
  level={1}
  style={{ color: tokens.colors.primary.main }}
>
  Заголовок
</Heading>
```

**Когда использовать:**

- ✅ Главные заголовки экранов
- ✅ Заголовки модальных окон
- ✅ Крупные заголовки секций

---

### Title - Заголовки секций и карточек

```tsx
import { Title } from '../ui';

// Large (~22px) - большие заголовки секций
<Title size="large">Заголовок секции</Title>

// Medium (~16px) - заголовки карточек
<Title size="medium">Название карточки</Title>

// Small (~14px) - мелкие заголовки
<Title size="small">Подзаголовок</Title>
```

**Когда использовать:**

- ✅ Заголовки карточек сотрудников
- ✅ Заголовки секций на экране
- ✅ Названия в списках
- ✅ Заголовки диалогов

**Примеры:**

```tsx
// Карточка сотрудника
<Card>
  <Title size="medium">Иванов Иван Иванович</Title>
  <Caption>Оператор ПВЗ</Caption>
</Card>

// Секция на экране
<View>
  <Title size="large" style={{ marginBottom: 12 }}>
    На смене сейчас
  </Title>
  {/* Контент секции */}
</View>
```

---

### Body - Основной текст

```tsx
import { Body } from '../ui';

// Large (~16px) - крупный текст
<Body size="large">Важная информация</Body>

// Medium (~14px) - основной текст (по умолчанию)
<Body>Основной текст приложения</Body>
<Body size="medium">Основной текст</Body>

// Small (~12px) - мелкий текст
<Body size="small">Дополнительная информация</Body>
```

**Когда использовать:**

- ✅ Основной контент экранов
- ✅ Описания в карточках
- ✅ Текст в диалогах
- ✅ Сообщения в чате

---

### Label - Метки и подписи

```tsx
import { Label } from '../ui';

// Large (~14px)
<Label size="large">Метка</Label>

// Medium (~12px) - основной размер меток
<Label>Должность:</Label>
<Label size="medium">ПВЗ:</Label>

// Small (~11px) - мелкие метки
<Label size="small">Статус</Label>
```

**Когда использовать:**

- ✅ Метки полей в формах
- ✅ Подписи к данным
- ✅ Текст в бейджах
- ✅ Текст кнопок фильтров

**Примеры:**

```tsx
// Поле формы
<View>
  <Label style={{ marginBottom: 4 }}>Email</Label>
  <TextInput placeholder="Введите email" />
</View>

// Метаданные
<View style={{ flexDirection: 'row', gap: 4 }}>
  <Label size="small">ПВЗ:</Label>
  <Body size="small">Тамбовская 41</Body>
</View>
```

---

### Caption - Вторичная информация

```tsx
import { Caption } from '../ui';

// Автоматически использует bodySmall + secondary цвет
<Caption>Обновлено 5 минут назад</Caption>

// С кастомным цветом
<Caption style={{ color: tokens.colors.text.muted }}>
  Дополнительная информация
</Caption>
```

**Когда использовать:**

- ✅ Временные метки
- ✅ Второстепенная информация
- ✅ Подписи к карточкам
- ✅ Описания под заголовками

**Примеры:**

```tsx
// Временная метка
<View>
  <Title size="medium">Задача выполнена</Title>
  <Caption>Сегодня в 14:30</Caption>
</View>

// Описание
<View>
  <Title size="large">График работы</Title>
  <Caption>Расписание смен на текущую неделю</Caption>
</View>
```

---

## Иерархия текста

### Уровень 1: Главные заголовки

```tsx
<Heading level={1}>  // 32px, bold
```

- Заголовки экранов
- Главные заголовки модалов

### Уровень 2: Подзаголовки

```tsx
<Heading level={2}>  // 28px, semibold
<Title size="large"> // 22px, semibold
```

- Подзаголовки экранов
- Заголовки крупных секций

### Уровень 3: Заголовки секций

```tsx
<Heading level={3}>  // 24px, semibold
<Title size="medium"> // 16px, semibold
```

- Заголовки секций
- Заголовки карточек
- Названия элементов списка

### Уровень 4: Основной текст

```tsx
<Body>              // 14px, regular
<Body size="large"> // 16px, regular
```

- Основной контент
- Описания
- Сообщения

### Уровень 5: Вторичный текст

```tsx
<Label>             // 12px, medium
<Caption>           // 12px, regular, secondary color
```

- Метки полей
- Подписи
- Временные метки
- Вспомогательная информация

---

## Когда использовать какой компонент

### Экран с заголовком и списком

```tsx
<View style={{ flex: 1, backgroundColor: tokens.colors.screenBackground }}>
  <ScrollView>
    {/* Главный заголовок экрана */}
    <Heading level={1} style={{ padding: 16 }}>
      Сотрудники
    </Heading>

    {/* Описание экрана */}
    <Caption style={{ paddingHorizontal: 16, marginBottom: 16 }}>
      Список всех сотрудников ПВЗ
    </Caption>

    {/* Секция */}
    <View style={{ padding: 16 }}>
      <Title size="large" style={{ marginBottom: 12 }}>
        На смене (5)
      </Title>

      {/* Карточка */}
      <Card>
        <Title size="medium">Иванов Иван</Title>
        <Caption>Оператор ПВЗ</Caption>
        <Body size="small" style={{ marginTop: 8 }}>
          Смена: 09:00 - 18:00
        </Body>
      </Card>
    </View>
  </ScrollView>
</View>
```

### Форма

```tsx
<View>
  <Heading level={2} style={{ marginBottom: 16 }}>
    Редактирование профиля
  </Heading>

  <View style={{ gap: 12 }}>
    {/* Поле 1 */}
    <View>
      <Label style={{ marginBottom: 4 }}>Имя</Label>
      <TextInput placeholder="Введите имя" />
    </View>

    {/* Поле 2 */}
    <View>
      <Label style={{ marginBottom: 4 }}>Email</Label>
      <TextInput placeholder="Введите email" />
      <Caption style={{ marginTop: 4 }}>Используется для входа в систему</Caption>
    </View>
  </View>
</View>
```

### Карточка с данными

```tsx
<Card>
  <Title size="medium" style={{ marginBottom: 8 }}>
    Финансы за месяц
  </Title>

  <View style={{ gap: 8 }}>
    <View style={{ flexDirection: 'row', justifyContent: 'space-between' }}>
      <Label>Начислено:</Label>
      <Body style={{ fontWeight: '600' }}>45 000 ₽</Body>
    </View>

    <View style={{ flexDirection: 'row', justifyContent: 'space-between' }}>
      <Label>Штрафы:</Label>
      <Body style={{ color: tokens.colors.error.main, fontWeight: '600' }}>-500 ₽</Body>
    </View>
  </View>

  <Caption style={{ marginTop: 12 }}>Обновлено сегодня в 10:00</Caption>
</Card>
```

---

## Цвета текста

### По умолчанию

Paper компоненты используют `theme.colors.onSurface` (чёрный текст)

### Кастомные цвета

```tsx
import { tokens } from '../ui';

// Primary текст (заголовки, основной текст)
<Title style={{ color: tokens.colors.text.primary }}>
  Заголовок
</Title>

// Secondary текст (описания, метаданные)
<Body style={{ color: tokens.colors.text.secondary }}>
  Описание
</Body>

// Muted текст (placeholder, второстепенное)
<Label style={{ color: tokens.colors.text.muted }}>
  Дополнительно
</Label>

// Disabled текст
<Body style={{ color: tokens.colors.text.disabled }}>
  Недоступно
</Body>

// Статусные цвета
<Body style={{ color: tokens.colors.success.main }}>
  Успешно
</Body>

<Body style={{ color: tokens.colors.error.main }}>
  Ошибка
</Body>
```

### Автоматический цвет (Caption)

```tsx
// Caption автоматически использует secondary цвет
<Caption>Вторичная информация</Caption>

// Эквивалентно:
<Body size="small" style={{ color: tokens.colors.text.secondary }}>
  Вторичная информация
</Body>
```

---

## Примеры использования

### Dashboard карточка

```tsx
<Card style={{ padding: 16 }}>
  {/* Заголовок */}
  <Title size="large" style={{ marginBottom: 4 }}>
    На смене сейчас
  </Title>

  {/* Описание */}
  <Caption style={{ marginBottom: 16 }}>5 сотрудников на рабочих местах</Caption>

  {/* Список */}
  <View style={{ gap: 12 }}>
    <View style={{ flexDirection: 'row', alignItems: 'center', gap: 8 }}>
      <Avatar.Text size={32} label="ИИ" />
      <View style={{ flex: 1 }}>
        <Body style={{ fontWeight: '600' }}>Иванов Иван</Body>
        <Caption>Оператор • Смена до 18:00</Caption>
      </View>
    </View>
  </View>
</Card>
```

### Диалог подтверждения

```tsx
<Dialog visible={visible} onDismiss={onClose}>
  {/* Заголовок */}
  <Heading level={3} style={{ padding: 16, paddingBottom: 8 }}>
    Завершить смену?
  </Heading>

  {/* Контент */}
  <View style={{ paddingHorizontal: 16, paddingBottom: 16 }}>
    <Body style={{ marginBottom: 12 }}>Вы уверены, что хотите завершить смену досрочно?</Body>

    <Caption>Незавершённые задачи будут переназначены</Caption>
  </View>

  {/* Действия */}
  <View style={{ flexDirection: 'row', gap: 8, padding: 16 }}>
    <Button mode="text" onPress={onClose}>
      Отмена
    </Button>
    <Button mode="contained" onPress={onConfirm}>
      Завершить
    </Button>
  </View>
</Dialog>
```

### Экран со статистикой

```tsx
<ScrollView style={{ flex: 1, backgroundColor: tokens.colors.screenBackground }}>
  <View style={{ padding: 16 }}>
    {/* Заголовок экрана */}
    <Heading level={1} style={{ marginBottom: 8 }}>
      Статистика
    </Heading>

    <Caption style={{ marginBottom: 24 }}>Показатели за текущий месяц</Caption>

    {/* Карточка со статистикой */}
    <Card style={{ padding: 16, marginBottom: 12 }}>
      <Title size="medium" style={{ marginBottom: 16 }}>
        Выполнено задач
      </Title>

      <View style={{ flexDirection: 'row', alignItems: 'baseline', gap: 4 }}>
        <Heading level={1}>248</Heading>
        <Body style={{ color: tokens.colors.success.main }}>+12%</Body>
      </View>

      <Caption style={{ marginTop: 8 }}>По сравнению с прошлым месяцем</Caption>
    </Card>
  </View>
</ScrollView>
```

---

## Migration Guide

### До (инлайн стили)

```tsx
// ❌ Старый подход
<Text style={{ fontSize: 16, fontWeight: '600', color: tokens.colors.text.primary }}>
  Заголовок карточки
</Text>

<Text style={{ fontSize: 13, color: tokens.colors.text.secondary }}>
  Описание
</Text>

<Text style={{ fontSize: 12, color: tokens.colors.text.secondary }}>
  Обновлено 5 минут назад
</Text>
```

### После (компоненты)

```tsx
// ✅ Новый подход
<Title size="medium">
  Заголовок карточки
</Title>

<Body size="small" style={{ color: tokens.colors.text.secondary }}>
  Описание
</Body>

<Caption>
  Обновлено 5 минут назад
</Caption>
```

### Преимущества

1. **Меньше кода:** Не нужно каждый раз указывать fontSize, fontWeight
2. **Консистентность:** Все заголовки одного уровня выглядят одинаково
3. **Масштабируемость:** Легко изменить размер всех заголовков глобально
4. **Читаемость:** Код более понятный и семантичный
5. **Paper Typography:** Используются правильные Paper variants

---

## Чек-лист перед использованием

- [ ] Определил уровень важности текста (заголовок/тело/метка)
- [ ] Выбрал подходящий компонент (Heading/Title/Body/Label/Caption)
- [ ] Выбрал правильный размер (level/size prop)
- [ ] Применил правильный цвет текста (primary/secondary/muted)
- [ ] Проверил, что текст читаем на фоне (контраст)

---

## Полезные ссылки

- [Color Usage Guide](COLOR_USAGE_GUIDE.md) - цвета
- [Layout Guide](LAYOUT_GUIDE.md) - принципы размещения
- [Design System](DESIGN_SYSTEM.md) - полная дизайн-система
- [Paper Typography Docs](https://callstack.github.io/react-native-paper/docs/guides/typography) - документация Paper
