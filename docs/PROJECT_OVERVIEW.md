# Проект WB ПВЗ — обзор (на 2025-10-01T11:30:28+07:00)

## Назначение
Мобильное приложение для управления персоналом пунктов выдачи заказов (ПВЗ) в Новосибирске: графики, учёт смен/подработок, премии/штрафы, коммуникация через «Чат».

## Пользователи и роли
- Владелец/Управляющий — общие отчёты, правила, управление точками.
- Администратор — расписание точки, чат, подтверждения, закрытие смен.
- Сотрудник — личный график, отметки смен, чат, финансы.

## Платформы
Android и iOS (Expo Managed + EAS Build; iOS — без Mac).

## Технологии (на 2025-10-01)
- React Native 0.81.4 (Expo SDK 54)
- React 19.1
- React Native Paper 5 (MD3)
- React Navigation 7.x (bottom-tabs, material-top-tabs, native-stack)
- Zustand 5 с persist + AsyncStorage
- Expo Notifications (инициализация в Expo Go отключена; для пушей нужен dev build)
- Дополнительно: expo-camera, expo-location, expo-image-picker, expo-document-picker, crypto-js, qrcode
- Инфраструктура сборки: Expo Application Services (EAS Build/Submit)
- Качество кода: ESLint, Prettier, TypeScript `--noEmit`

## Дизайн‑система
- Источник: `DESIGN_SYSTEM.md`
- Токены: `src/ui/themeTokens.ts` (radius=8, controlHeight=48, buttonHeight=44, gap=12)
- Тема: `src/theme.ts` (MD3, кастомные цвета, placeholderColor, outline)
- Унифицированные компоненты: `src/components` (`StyledButton`, `StyledCard`, `StyledDialog`, `StyledScrollView`, `SquareIconButton` и др.)

## 🎯 Текущие модули (статус на 2025-10-01)

### ✅ Протестировано и работает:

#### Моя смена — Обзор (MVP) ✅
- **Начало смены**: QR-сканирование с проверками (TTL, HMAC, геолокация 100м)
- **Перерывы**: 3×10 минут с автозавершением по таймеру
- **Завершение смены**: умный диалог с предупреждениями (без жесткой блокировки)
- **Лог событий**: история действий с временными метками
- **Статус**: 🟢 Протестировано и работает отлично

#### Моя смена — Задачи (MVP) ⏳
- Чек-листы и поручения с фото
- Синхронизация бэйджей с количеством невыполненных
- **Статус**: ⏳ Требует тестирования

### ⏳ Требуют тестирования:

#### График (MVP) ⏳
- 5 недель (текущая +4), разделители месяцев
- Режим редактирования с запретами на прошлые даты
- Навигация «Сегодня/←/→»
- **Статус**: ⏳ Требует тестирования

#### Финансы (MVP) ⏳
- Текущий период: суммарные показатели, детали
- История (би-вайкли), экспорт (MVP), «Оспорить»
- **Статус**: ⏳ Требует тестирования

#### Чат (MVP) ⏳
- Список диалогов, экран переписки
- Вложения: камера, галерея, документы
- Выдвижная панель смайлов, динамическая кнопка (скрепка/отправка)
- **Статус**: ⏳ Требует тестирования

### 🔧 Технические компоненты:

#### Посещение / QR ✅
- QR с TTL+HMAC, геозона 100 м
- Генерация локально и через `test-qr.html` (автоматическая с CryptoJS)
- Верификация подписи через `utils/qr.ts`
- **Серверная валидация временно отключена** для тестирования
- **Статус**: 🟢 Работает локально

## Навигация
- Сотрудник: Моя смена (верхние табы: Задачи · Обзор) · График · Финансы · Чат.
- Администратор: Главная · Сотрудники (Список/График/Расчёт) · Чат (каркас).
- Владелец: Дашборд · ПВЗ · Правила (каркас).

## Мини‑модель данных (UI слой)
`EmployeeRequest`: id, title, description, type (overtime|shift_swap|penalty_dispute|...), status, createdAt, pvzId?, shiftId?, attachments[], employeeName?

## 📋 Система тестирования (создана 2025-10-01)

### Структура `docs/test-specs/`:
- `README.md` — описание системы, статусы, инструкции
- `TEST_SESSION_TEMPLATE.md` — шаблон для сессии тестирования
- `user-flows/` — 7 детальных пользовательских сценариев

### Статус тестов:
- 🟢 01_employee_shift_start.md — **PASSED**
- 🟢 02_employee_shift_end.md — **PASSED**
- 🟢 03_employee_breaks.md — **PASSED**
- ⏳ 04_employee_tasks.md — NOT TESTED
- ⏳ 05_employee_schedule.md — NOT TESTED
- ⏳ 06_employee_finance.md — NOT TESTED
- ⏳ 07_employee_chat.md — NOT TESTED

## 📋 Правила работы с сессиями
- `SESSION_RULES.md` — актуальная инструкция.
- При заполнении контекста > 80%: команда «Подготовься к началу новой сессии».

## Как запустить
- Dev: `npm run android`, `expo start --tunnel` (при необходимости туннеля).
- Локальный TS/Lint: `npm run typecheck`, `npm run lint`.
- Expo dev client (dev build): `npx expo run:android --device`.
- Сборки: `npm run build:android`, `npm run build:ios`.
- Генерация QR для тестов: `npm run qr pvz-dev 300`.

## Контроль качества
- Визуальные стандарты — `DESIGN_SYSTEM.md`.
- Плейсхолдеры — серый `#6B7280`.
- Гео/камера запрашиваются только по действию пользователя.
- Линт/формат/тайпчек обязательны перед коммитами.

## 🎉 Оптимизации и улучшения (выполнено)

### Последняя сессия (2025-10-01):
- ✅ **Очистка проекта**: удалены бэкапы, временные файлы, дубликаты (~200+ файлов)
- ✅ **Обновлен .gitignore**: правила для build-артефактов, бэкапов, тестовых скриптов
- ✅ **Исправлен app.config.ts**: убран конфликт `newArchEnabled: false` с Expo Go
- ✅ **Улучшен UX завершения смены**:
  - Убрана жесткая блокировка по времени (было: только после 22:00)
  - Добавлены умные предупреждения о незавершенных задачах
  - Информация о раннем завершении (если до планового времени)
  - Красивые цветные блоки в диалоге (желтое предупреждение + синяя информация)
- ✅ **Исправлена ошибка консоли**: Fragment props в StyledDialog
- ✅ **Создана система тестирования**: полная структура с 7 детальными сценариями
- ✅ **Протестирован MVP "Моя смена"**: начало, перерывы, завершение — всё работает

### Предыдущие оптимизации:
- Полная миграция UI на унифицированные компоненты (Paper + токены).
- Общие утилиты для QR и геолокации (`src/utils/qr.ts`, `src/utils/geo.ts`).
- HMAC-SHA256 (crypto-js) и проверка подписи QR.
- Zustand persist с AsyncStorage для `auth`, `shift`, `requests`.
- ESLint/Prettier/TypeScript скрипты настроены.
- Expo конфиг вынесен в `app.config.ts`, `eas.json` содержит профили `development/preview/production`.

## 📝 Планы (кратко)

### Высокий приоритет:
1. ✅ ~~Протестировать "Моя смена" (Обзор)~~ — **ВЫПОЛНЕНО**
2. ⏳ Протестировать остальные модули (Задачи, График, Финансы, Чат)
3. ⏳ Собрать development build для тестирования уведомлений
4. ⏳ AuthNavigator: полноценные Paper-формы входа/выхода

### Средний приоритет:
5. График: экран «Мои заявки», сохранение изменений, интеграция с бэкендом.
6. PvzSettings: карта (react-native-maps), редактирование геозоны, подготовка к Supabase.
7. Чат: поиск, предпросмотр вложений, статусы доставки, отправка по Enter.

### Низкий приоритет (после MVP):
8. Настроить pvzqr.ru (PHP API: validate.php, bind.php)
9. Supabase: схема (auth, график, заявки, чат), RLS, миграции.
10. DevOps: Husky/CI, релизные чек-листы, `.env.production`.

## 🚀 Следующая сессия должна начаться с:
1. **Чтение** `CURSOR_CONTEXT.md` для быстрой ориентации
2. **Проверка** `TASKS.md` для понимания приоритетов
3. **Продолжение тестирования** с `docs/test-specs/user-flows/04_employee_tasks.md`
