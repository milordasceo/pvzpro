# 📱 PVZ Pro — Контекст проекта

> Последнее обновление: 26 декабря 2025, 21:48

## 🎯 Цель проекта

**PVZ Pro** — мобильное приложение для сотрудников пунктов выдачи заказов (ПВЗ) маркетплейсов (Wildberries, Ozon, Яндекс Маркет). 

Приложение помогает:
- Отслеживать рабочее время (смены, перерывы).
- Управлять задачами на смене.
- Контролировать финансы (зарплата, премии).
- Общаться с командой.

---

## 👥 Роли пользователей

| Роль | Описание | Статус |
|------|----------|--------|
| **Сотрудник** | Работник ПВЗ. Основной пользователь. | ✅ В разработке |
| **Менеджер** | Управляющий несколькими ПВЗ. | 🕐 Планируется |
| **Владелец** | Владелец сети ПВЗ. Доступ к аналитике. | 🕐 Планируется |

---

## 🏗 Архитектура

### Структура проекта (Feature-Sliced Design)

```
src/
├── features/           # Бизнес-логика по фичам
│   ├── auth/           # Авторизация
│   │   └── model/      # auth.store.ts
│   └── employee/       # Функционал сотрудника
│       ├── model/      # shift.store.ts, mocks.ts, useShiftTimer.ts
│       └── screens/    # ShiftScreen, ProfileScreen, TasksScreen...
├── navigation/         # Навигация
│   ├── stacks/         # EmployeeNavigator.tsx
│   └── tabs/           # EmployeeTabNavigator.tsx
└── shared/             # Общие компоненты
    ├── hooks/          # useGeolocation.ts
    ├── theme/          # colors.ts, layout.ts
    ├── ui/             # Button.tsx, SlideToAction.tsx
    └── utils/          # error-handler.ts, styles.ts
```

### Стек технологий

| Категория | Технология |
|-----------|------------|
| Framework | React Native 0.81+ (Expo SDK 54) |
| Navigation | React Navigation (Native Stack + Bottom Tabs) |
| State | Zustand |
| Animations | React Native Reanimated v4 |
| Icons | Lucide React Native |
| Haptics | expo-haptics |
| Camera/Gallery | expo-image-picker, expo-camera |
| Location | expo-location |

---

## 📱 Экраны приложения

### Сотрудник (Employee)

| Экран | Файл | Описание | Статус |
|-------|------|----------|--------|
| **Смена** | `ShiftScreen.tsx` | Главный экран. Таймер смены, перерывы, начало/завершение работы. | ✅ Готов |
| **Профиль** | `ProfileScreen.tsx` | Данные пользователя, редактирование, поддержка, выход. | ✅ Готов |
| **Задачи** | `TasksScreen.tsx` | Чек-листы и задачи на смену, фото-отчёты. | ✅ Готов |
| **Финансы** | `FinanceScreen.tsx` | Зарплата, премии, штрафы. | 🟡 Заглушка |
| **Расписание** | `ScheduleScreen.tsx` | Календарь смен. | 🟡 Заглушка |
| **Чат** | `ChatScreen.tsx` | Общение с командой. | 🟡 Заглушка |
| **Настройки** | `SettingsScreen.tsx` | (Не используется, функционал в Профиле) | ⚪ Резерв |

---

## 🎨 Дизайн-система

### Цвета

Основные цвета определены в `src/shared/theme/colors.ts`:

- **primary** — основной акцент (синий).
- **success** — успех (зелёный).
- **warning** — предупреждение (жёлтый/оранжевый).
- **danger** — ошибка/опасность (красный).

### Темы маркетплейсов

```typescript
marketplaceThemes = {
  wb: { primary: '#CB11AB', ... },      // Wildberries (фиолетовый)
  ozon: { primary: '#005BFF', ... },    // Ozon (синий)
  yandex: { primary: '#FFCC00', ... },  // Яндекс (жёлтый)
}
```

### Типографика

Определена в `theme.typography.presets`:
- `h1`, `h2`, `h3`, `h4` — заголовки.
- `body`, `bodySmall` — основной текст.
- `label`, `caption` — мелкий текст.

---

## 🔑 Ключевые компоненты

### `SlideToAction`
Слайдер для критических действий (начать/завершить смену). Защита от случайных нажатий.

**Файл:** `src/shared/ui/SlideToAction.tsx`

### `useGeolocation`
Хук для геолокации. Проверяет, находится ли сотрудник в радиусе ПВЗ.

**Файл:** `src/shared/hooks/useGeolocation.ts`

### `useShiftTimer`
Хук для таймера смены и перерывов.

**Файл:** `src/features/employee/model/useShiftTimer.ts`

---

## 📋 Что сделано (v2.4.12)

### Смена (ShiftScreen)
- ✅ Минималистичный редизайн.
- ✅ Таймер смены (часы:минуты:секунды).
- ✅ Управление перерывами (до 3 шт).
- ✅ Геолокация при старте смены.
- ✅ SlideToAction для начала/завершения.
- ✅ Навигация в профиль через клик на имя.

### Профиль (ProfileScreen)
- ✅ Hero-секция (аватар, имя, роль, рейтинг).
- ✅ Редактирование: имя, телефон, email.
- ✅ Смена аватара (камера/галерея).
- ✅ Блок "Помощь" (поддержка, FAQ).
- ✅ Информация о приложении.
- ✅ Выход с подтверждением.

### Задачи (TasksScreen)
- ✅ Единый список задач (без вкладок).
- ✅ Аккордеон-карточки (нажатие раскрывает детали).
- ✅ Два типа: чек-листы (несколько шагов) и подсчёт (ввод числа).
- ✅ Прогресс-бар для чек-листов.
- ✅ Фильтр: Все / В работе / Готово.
- ✅ Фото-отчёты для каждого пункта.
- ✅ Дедлайны задач.
- ✅ Автосброс задач при завершении смены.

### Навигация
- ✅ Tab Bar (5 вкладок: Смена, Задачи, Расписание, Финансы, Чат).
- ✅ Stack Navigator (Tabs → Profile → Settings).
- ✅ Badge на вкладке "Задачи" (количество pending).

### Удалено
- ❌ QR-код сканер (заменён на геолокацию).
- ❌ Переключатель маркетплейсов из профиля (dev-функция).

---

## 🚀 Планы (TODO)

### Ближайшие задачи
1. **Модуль "Расписание"** — календарь смен.
2. **Push-уведомления** — напоминания о смене.
3. **Модуль "Финансы"** — зарплата, премии, штрафы.

### Дальнейшее развитие
- Роль "Менеджер".
- Роль "Владелец" с аналитикой.
- Интеграция с API маркетплейсов.

---

## 🔧 Разработка

### Запуск
```bash
npx expo start --android
```

### Сборка
```bash
eas build --platform android
```

### Git Flow
- **main** — production.
- **develop** — активная разработка (коммиты сюда).
