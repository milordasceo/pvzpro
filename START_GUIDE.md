# 🚀 Быстрый запуск приложения

> **Просто запустите `START.bat` - всё запустится автоматически!**

---

## ⚡ Запуск

**Дважды кликните:** `START.bat`

**Всё запускается автоматически:**
1. ✅ scrcpy (зеркалирование экрана)
2. ✅ Metro Bundler
3. ✅ Приложение на устройстве

**Время:** ~12 секунд до полного запуска ⚡

---

## 🎯 Что происходит автоматически

```
[1/6] Cleaning ports...
      → Останавливает только процессы на портах 8081/8097
      → НЕ трогает scrcpy и другие процессы!
      → Закрывает старое приложение на устройстве

[2/6] Starting scrcpy...
      → Запускает зеркалирование экрана
      → Окно устройства на вашем мониторе
      → Управление мышью и клавиатурой

[3/6] Starting ADB...
      → Проверяет подключение устройства
      → Запускает ADB сервер

[4/6] Port forwarding...
      → adb reverse tcp:8081 tcp:8081
      → adb reverse tcp:8097 tcp:8097

[5/6] Starting Metro...
      → Запускает Metro Bundler с --localhost
      → Ожидание инициализации 7 секунд

[6/6] Opening app...
      → Автоматически открывает Expo Go
      → Начинает сборку приложения
```

---

## 🔧 Требования

### 1. ADB в PATH
**Расположение:**  
`C:\Users\ВашеИмя\AppData\Local\Android\Sdk\platform-tools`

**Проверка:**
```bash
adb devices
```
Должно показать ваше устройство.

### 2. scrcpy (опционально)
**Установка:**
```bash
winget install scrcpy
```

**Если scrcpy не установлен:**
- Скрипт продолжит работу без зеркалирования
- Metro и приложение запустятся нормально

---

## 💡 Преимущества scrcpy

- 📱 Видите устройство на большом мониторе
- ⌨️ Управление мышью и клавиатурой  
- 📸 Легко делать скриншоты
- 🎥 Запись видео
- ⚡ Низкая задержка (~30ms)
- 🔋 Экран устройства не гаснет (stay-awake)

---

## 💻 Горячие клавиши

### В Metro (консоль):
- **`r`** - Перезагрузить приложение
- **`a`** - Открыть на Android
- **`Ctrl+C`** - Остановить Metro

### В scrcpy (окно устройства):
- **`Ctrl+C`** - Копировать (из устройства в ПК)
- **`Ctrl+V`** - Вставить (из ПК в устройство)
- **`Ctrl+F`** - Полноэкранный режим
- **`Ctrl+O`** - Выключить экран устройства
- **`Ctrl+S`** - Сделать скриншот
- **`Ctrl+R`** - Запись видео (старт/стоп)

---

## 📊 Умная очистка портов

Скрипт использует **селективную** очистку:

✅ **Останавливает:**
- Процессы на портах 8081 и 8097
- Старое приложение Expo на устройстве

❌ **НЕ трогает:**
- scrcpy
- Другие Node.js процессы
- VSCode / другие IDE
- Браузеры и прочие приложения

---

## ❓ Решение проблем

### "ADB not found"
**Решение:** Добавьте `platform-tools` в PATH
```
C:\Users\%USERNAME%\AppData\Local\Android\Sdk\platform-tools
```

### "No devices found"
**Решение:**
1. Включите **USB debugging** в настройках разработчика
2. Подключите устройство по USB
3. Разрешите отладку на устройстве (появится диалог)

### "scrcpy not found"
**Не критично!** Скрипт продолжит работу без зеркалирования.

**Для установки:**
```bash
winget install scrcpy
```

### Приложение не открывается
**Решение:**
1. Установите **Expo Go** из Play Store
2. Проверьте USB debugging
3. Запустите `START.bat` снова

### Metro не запускается / порты заняты
**Просто запустите `START.bat` снова!**

Скрипт автоматически:
- ✅ Освободит порты 8081 и 8097
- ✅ Закроет старое приложение
- ✅ Перезапустит всё с чистого листа
- ✅ **scrcpy останется работать!**

### scrcpy показывает черный экран
**Решение:**
1. Проверьте, что устройство **разблокировано**
2. Закройте окно scrcpy и перезапустите `START.bat`
3. Разрешите передачу изображения на устройстве

### Приложение не обновляется
**Решение:**
1. Нажмите **`r`** в Metro для перезагрузки
2. Или потрясите устройство → **Reload**
3. Или в scrcpy нажмите **`Ctrl+R`** (shake gesture)

---

## 🎓 Советы по работе

### Разработка UI
→ Используйте **scrcpy окно**
- Быстрее видите изменения
- Удобнее для скриншотов
- Можно работать за компьютером

### Тестирование жестов
→ Работайте с **физическим устройством**
- Реальные тач-взаимодействия
- Тестирование свайпов/жестов
- Проверка тактильных откликов

### Демонстрация клиенту
→ Используйте **scrcpy + запись видео**
- Нажмите `Ctrl+R` для начала записи
- Покажите функциональность
- `Ctrl+R` снова для остановки
- Видео сохранится на ПК

---

## 🛑 Остановка

**Для остановки Metro:**
- Нажмите **`Ctrl+C`** в консоли

**Для закрытия scrcpy:**
- Просто закройте окно scrcpy
- Экран устройства включится автоматически

**Всё очистится автоматически** при следующем запуске!

---

## 🚀 Альтернативный запуск (через npm)

Если нужно запустить только Metro без scrcpy:

```bash
# Настроить порты вручную
adb reverse tcp:8081 tcp:8081

# Запустить Metro
npm run dev          # С очисткой кэша
npm start            # Без очистки

# Открыть на устройстве
npx expo start --localhost
# нажмите 'a'
```

**Но `START.bat` проще и быстрее!** 🎉

---

## 📝 Что внутри START.bat

Скрипт делает именно то, что нужно для разработки:

1. **Умная очистка** - только нужные порты
2. **scrcpy** - зеркалирование экрана (опционально)
3. **ADB setup** - проверка и настройка
4. **Metro** - автозапуск в фоне
5. **Expo** - автооткрытие на устройстве
6. **Логи** - видны прямо в консоли

**Всё в одном месте, всё автоматически!**

---

**Готово! Просто запускайте `START.bat` каждый раз! 🎉**
